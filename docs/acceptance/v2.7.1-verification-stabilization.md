# 2.7.1 verification stabilization — exact authority rebaseline and evidence

Scope: the bounded 2.7.1 stabilization candidate on base
`a79b6c07035b10b9ed406fa8843ac6a5293a0bda`. Two verifying-pipeline stop causes
are removed: the stale active-authority ratchet and the divergence between the
public `retry-mechanism-flake` decision and a separate manual resubmit
command. This note claims no review, reap, plugin installation, dogfood, tag,
or publication.

## Exact final authority rebaseline

Rerunnable, subject-HEAD-independent commands (run from the repository root of
the exact candidate tree):

- `python3 scripts/code-quality-audit.py --rc1-authority-json`
- `python3 tests/test_code_quality_audit.py`
- `python3 tests/harness/test_v266_rc1_integration.py`

Measured contour of the final candidate (the same separately owned 27-file
manifest in `config/active-review-authority.json`; membership unchanged):

```json
{
  "production_loc": 15051,
  "production_file_count": 27,
  "writable_authority_count": 0,
  "incident_literal_count": 0
}
```

`config/code-quality-baseline.json` `rc1_active_authority.final_max_production_loc`
is bound to exactly `15051` — the measurement of this candidate, not a growth
allowance. `tests/harness/test_v266_rc1_integration.py` now asserts exact
equality on `production_loc`, so later production-authority growth **and**
stale headroom are both immediately visible:

- On the approved base the assertion was RED: measured `15051` against the
  stale `14992` ceiling inherited from the 2.6.6 RC1 rebaseline.
- With the equality assertion in place before the rebaseline, a stale lower
  ceiling (`14992 -> RED`) and stale headroom (`15100 -> RED`) are both
  rejected; only the exact measurement (`15051 -> GREEN`) passes.

## Why this rebaseline admits stabilization, not a new authority surface

The `59` LOC of growth over the stale ceiling accrued inside already-owned
contour files across 2.6.7 RC stabilization (chiefly
`scripts/harness/runtime_worker_verification.py`,
`scripts/task_review_verification_resubmit.py`, and
`scripts/task_escalation_records.py`); no file was added to or removed from
the 27-file manifest, and the 2.7.1 slice itself keeps the contour at the same
`15051` LOC it measured before the slice: the canonical-decision repair lives
in the non-contour owners `scripts/task_escalation.py`,
`scripts/harness/artifact_repair.py`, and
`scripts/pipeline-verification-resubmit.py`, and the one contour edit
(deleting the superseded manual same-HEAD resubmit prose from the
verification attention message) is line-neutral. Writable authorities and
incident literals remain zero, and no new production authority module or
compatibility path exists.

## Canonical retry decision and automatic same-HEAD response

`retry-mechanism-flake` is the single public decision from typed raise through
the durable resolution record and the coordinator wake; the private
`authorize-one-same-head-retry` alias is retired and fails closed as an input
token. Only the typed constructor in `scripts/harness/artifact_repair.py`
translates the public decision into the private `authorize-attempt-1` action.
Resolving the exact decision from the originating coordinator session invokes
the registered idempotent response builder in
`scripts/pipeline-verification-resubmit.py` once before waking the task, so
attempt 1 is consumed without a model resubmit command; the manual
`--same-head-mechanism-flake` CLI no longer exists. Attempt-0 evidence, the
one-retry ceiling, and changed-HEAD fix-and-resubmit are unchanged.

Focused evidence:

- `python3 tests/test_pipeline_verification_resubmit.py`
- `python3 tests/harness/test_verification_attempt.py`
- `python3 tests/harness/test_artifact_repair.py`
- `python3 tests/harness/test_runtime_task_summary.py`
