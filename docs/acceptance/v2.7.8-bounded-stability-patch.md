# LLM Obsidian v2.7.8 bounded stability patch — candidate evidence

Scope: three narrow repairs on exact target base
`379e772c90490a68a731f7533e0d2647dc3e9cc5`. The candidate adds no scheduler,
lifecycle or store owner, public schema, dependency, migration, provider
fallback, retry budget, callback replay, network replay, or live acceptance.

## E278.ONE_SHOT_CLEANUP

The real-core fixture in `tests/harness/test_runtime_sessions.py` launches a
protected research fetch with its prompt in provider argv and proves all of the
following in one operation lineage:

1. the exact prompt persisted in research argv matches the prompt pointer bytes
   before the existing provider-event stream accepts input, without
   `cmux.send` or `send_key`;
2. the callback broker accepts the exact result once;
3. the stream records, in order, `provider-started`, `input-accepted`,
   `result-published`, `process-exited`, and `resource-closed` for the exact
   ready generation;
4. cleanup releases every owned resource and creates the synthesis child;
5. the provider argv is invoked once and no second prompt or network fetch is
   performed.

The adjacent research vertical, runtime research, and isolation suites retain
their existing fail-closed behavior.

## E278.ARGV_PROMPT_DIGEST

The real worker regression prepares a research launch with one prompt in the
persisted argv, starts the actual provider process, then mutates only the prompt
pointer while the durable `start-provider` effect is still held. Releasing that
effect proves the worker compares the pointer bytes with the already persisted
argv bytes before it creates or accepts the provider-input event. The mismatch
uses existing provider containment, moves the operation to
`attention-required`, exits as `input-unconfirmed`, and publishes no
`input-accepted` event. No launch schema, public API, dependency, replay,
deadline, or budget changes are involved.

## E278.STALE_ESCALATION

`tests/harness/test_task_escalation_records.py` constructs a real frozen custom
pipeline and the same predecessor-complete, successor-awaiting operation-store
state that previously triggered suppression. An untyped `mechanism-failure`
raise now follows the ordinary public path: it appends a durable pending record,
publishes the attention pointer, and performs one notification and one
coordinator send. The state-only classifier and its unused framework are gone.

This deliberately defers `v278-suppress-stale-post-self-heal-escalation`.
Current lifecycle state cannot distinguish a delayed predecessor raise from a
genuine successor failure without exact incoming operation, run, visit,
callback, and successor identity. No public escalation contract is added in
this patch.

## E278.VERIFICATION_CONTROL_CLEANLINESS

`tests/test_dispatch_runner.py` exercises the real linked-worktree setup,
creates `.task-verification.json`, `.task-verification-response.json`, and
`.task-verification-contract.json`, and observes their scoped
`git status --porcelain`. Before the repair all three appeared as untracked
product dirt. The repair enables Git worktree config and binds
`core.excludesFile` to an exclude file inside the dispatched worktree's own Git
directory. Every task-owned pattern is root-anchored and installed once.

The linked-worktree regression proves that the three root controls are hidden
only in the dispatched worktree: a sibling worktree still reports the same
root basenames, nested same-basename files remain visible, and unrelated common
exclude entries remain unchanged. The repair does not weaken
`product_tree_is_clean()` or write task patterns into the repository's shared
`$GIT_COMMON_DIR/info/exclude`. This closes the release-dogfood failure in which
all attempt-1 probes passed at one exact HEAD but receipt publication rejected
the retry's own control transport as product drift.

## E278.QUALITY

The target active-authority denominator remains the exact existing 27-file
manifest:

- production LOC: 15,603;
- writable authorities: 0;
- incident literals: 0;
- `runtime_session_launch.py`: 1,155 lines under the existing 1,157 ceiling;
- all production Python under `scripts/`: 290 files / 109,370 lines, the exact
  measured candidate and 278 lines below the unchanged 109,648-line ceiling.

The 15,603→15,663 drift diagnosed during planning is a cross-repository fact,
not target growth. Commit `71f0b27c781851c8aadf762d168654586ae2a11f`
exists in `llm-obsidian-swarm` and incorporates a 60-line liveness repair over
the 15,603 parent contour. The target base `379e772c` neither contains that
commit nor its `runtime_worker_liveness.py` change. Coordinator decision
`dd1554aa-48c5-41fd-a53b-56dcd58cc764` therefore preserves the target's actual
15,603 ceiling, ports no swarm-only code, and creates no target headroom.

## E278.RELEASE_DOCS

`CHANGELOG.md`, `CHANGELOG.ru.md`, and `docs/releases/v2.7.8.md` describe the
retained cleanup and cleanliness repairs, the exact argv/pointer binding, the
stale-suppression deferral, the repository-bound baseline clarification,
measured ratchet contraction, and the unperformed release operations. They make
no claim of tag, push, publication, installation, or live dogfood.

## Candidate gate

Run the focused and full checks from one exact committed candidate:

```text
python3 tests/harness/test_runtime_sessions.py
python3 tests/harness/test_research_vertical.py
python3 tests/harness/test_runtime_research.py
make test-research-isolation
python3 tests/harness/test_task_escalation_records.py
python3 tests/harness/test_custom_runtime.py
python3 tests/harness/test_dispatch_runtime.py
python3 tests/harness/test_runtime_task_summary.py
python3 tests/harness/test_v266_rc1_integration.py
python3 tests/test_dispatch_runner.py
python3 tests/rc4_scope_ratchet.py
python3 scripts/code-quality-audit.py --json
make test-harness
make test
make test-code-quality
python3 scripts/validate-vault.py --summary
make acceptance-check
python3 scripts/codex-adapter.py --check
scripts/mcp-gateway/mcp-gateway.sh codex-sync --check
python3 -m py_compile scripts/dispatch_contracts.py scripts/harness/runtime_session_launch.py scripts/harness/runtime_worker_execution.py scripts/harness/runtime_worker_custom.py scripts/task_escalation.py tests/harness/test_runtime_sessions.py tests/harness/test_task_escalation_records.py tests/test_dispatch_runner.py
git diff --check
```

The Harness verification receipt binds the exact clean candidate HEAD outside
this commit, avoiding a self-referential documentation amendment. Exact-HEAD
review and reap remain coordinator-owned after the canonical task summary.
