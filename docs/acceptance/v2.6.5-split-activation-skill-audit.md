# LLM Obsidian 2.6.5: Split activation skill audit

## Boundary

The Subplan D audit remains immutable historical evidence for the disabled,
preview-only component. Stability Gate S subsequently passed on clean exact
HEAD `023f69320b0833f8aac33a69f03d387ea2e485c1`, so the approved Join plan
authorizes one new explicit branch: `$split --dispatch`.

Default `$split` still ends after `split-preview.py` returns an exact sealed
manifest, accepted validation and four zero effect counters. Ordinary requests
to divide prose, files or data remain router false positives. Activation
requires the explicit flag and an approved plan that authorizes child effects.

## Five passes

| Pass | Ruling | Evidence |
|---|---|---|
| Invocation | pass | explicit-only metadata remains aligned; `--dispatch` is a distinct strong-intent branch |
| Information hierarchy | fix | the old normal path ended at preview and could not route the approved post-Gate capability |
| Steering | fix | activation now has exact validate, wave, lifecycle and join completion checks |
| Pruning | pass | provider/cmux mechanics remain in the harness; the skill only names public facades and stop rules |
| Goal preservation | pass | local preview/activation proxies stay subordinate to J2–J4 and the parent Outcome Contract |

The permission boundary is unchanged for preview. The activation branch does
not gain direct model or cmux authority: `split-runner.py` calls the existing
dispatch adapter, while lifecycle operations remain exclusively under
`harness-cli.py`. Invalid manifests, task-policy drift, unapproved terminal
receipts, stale HEADs and open resources all stop without another child effect.

## Deterministic evidence

- `python3 skills/improve-skills/scripts/audit_skills.py --strict`
- `python3 skills/improve-skills/scripts/audit_skills.py --verdicts docs/acceptance/v2.6.5-split-activation-skill-verdicts.json --scope split --strict`
- `make test-instruction-lint test-skill-budget test-codex-adapter`
- `python3 tests/test_split_components.py`
- `python3 tests/harness/test_split_activation.py`
- `python3 scripts/release-acceptance.py check`

These checks establish the skill and activation boundary only. Release
completion additionally requires exact-HEAD final verification, bounded live
single-model dogfood, provider availability disposition and terminal resource
inventory.
