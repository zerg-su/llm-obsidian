# LLM Obsidian 2.6.6 RC1 release readiness

## Candidate contract

The authoritative technical candidate is exact clean HEAD
`126b5fecb087a231bd6fbec8ce3f5dfe9235a206`. Immutable
[`release-final` attempt `26660000-0000-4000-8000-000000000002`](evidence/v2.6.6-rc1/replacement-126b5fe/receipt.json)
binds that commit and tree, all command arguments, output digests, runner and
profile identities, timing, exit codes, coverage denominator, transition-case
count, resource inventory, diff check and clean status.

The first candidate `fbf87a4e8ef532b43e4d55225c87ad0f39f55bd9`
passed the full suite but stopped at coverage because two zero-caller legacy
adapters remained in the canonical audit inventory. Its retained
[`diagnostic-only` receipt](evidence/v2.6.6-rc1/failed-fbf87a4/diagnostic-receipt.json)
is not release evidence. The one allowed product repair deleted those adapters
and changed no coverage floor. The replacement candidate passed; no further
candidate cycle is authorized.

## E1-E8 evidence map

| Evidence | RC1 disposition and proof |
|---|---|
| E1 negative active surface | `scripts/code-quality-audit.py --rc1-authority-json` reports no writable authority and no incident-pinned literal. The bounded active contour fell from 9,326 to 3,867 production lines; the branch removes 20,895 lines overall. |
| E2 exact-HEAD terminal authority | `test_exact_head_review_attempt.py` proves terminal/idempotent exact attempts, changed-HEAD fresh reservation, and inspect-only pre-activation history. Cross-HEAD rearm/recovery code is deleted. |
| E3 event-only review effects | `test_event_only_review_effects.py` proves that elapsed time, screen stability and dead/unknown process state authorize no positive reviewer effect; one exact first `turn-stopped` event authorizes one submit, then repetition stops at attention. Reviewer nudge/restart ceilings are zero. |
| E4 sealed Split base | Dispatch resolves and persists `base_sha` before effects. Split contracts, manifests, terminal receipts and Join ancestry use that SHA; moved/deleted refs still work and unrelated history fails. |
| E5 portable review packet | Bounded diff truncation operates on UTF-8 bytes and decodes only a valid prefix, with multibyte boundary regressions. |
| E6 incremental ladder | [`v2.6.6-rc1-slice-receipts.json`](v2.6.6-rc1-slice-receipts.json) binds RED, GREEN and focused commands to exact ancestor commits for A-G and audits that no broad command preceded integration GREEN. |
| E7 RC2 containment | [`v2.6.6-rc1-findings.json`](v2.6.6-rc1-findings.json) classifies every retained and release-gate finding. [`v2.6.6-rc2-defect-ledger.json`](v2.6.6-rc2-defect-ledger.json) remains schema-valid and empty because no additional adjacent defect was observed. |
| E8 exact candidate proof | The immutable replacement receipt is valid for exact candidate `126b5fe`, with full tests, 74.14% honest harness coverage (`16,755/22,599`), every standing critical floor, 4,370 generated transitions, acceptance/vault/quality/skill/instruction/adapter/MCP gates, zero Harness resources, doctor `ok`, and clean diff/status. The task contract launches the one required bounded single-model deep review from the canonical task summary. |

## Release boundary

This establishes the RC1 technical candidate and submits it to the required
harness-owned single-model review. Descendant commits may add only immutable
receipt bindings, this release-readiness page and release notes; they do not
change the tested product tree. A material review finding must be resolved in
a new commit through the existing exact-HEAD finalization contract. No push,
tag, publish, deploy, installation or release action is part of this task.
