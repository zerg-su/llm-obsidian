# LLM Obsidian 2.6.5: Split skill governance audit

## Initialization and inventory

System `skill-creator` initialized `skills/split/` before its template was
replaced. The repository inventory then contained 35 skills. The candidate is
explicit-only in both Claude metadata and `agents/openai.yaml`; its four-byte
description keeps the existing 7,500-byte Codex registry ceiling unchanged.

The generic system `quick_validate.py` was unavailable because its host Python
lacks PyYAML, and its generic allowed-key set also excludes this repository's
canonical `disable-model-invocation` field. No dependency or metadata boundary
was changed. The repository-authoritative `audit_skills.py --strict` validated
frontmatter, name parity, explicit-invocation parity, links, placeholders and
body size with zero errors or warnings.

## Five passes

| Pass | Ruling | Evidence |
|---|---|---|
| Invocation | pass | only explicit `$split`; ordinary splitting is a false positive and cannot implicitly invoke |
| Information hierarchy | pass | freeze → propose → preview → report is the complete normal path; no conditional reference is required |
| Steering | pass | every step ends in an exact checkable completion condition |
| Pruning | pass | 45 body lines; no duplicated implementation/schema or speculative dispatch instructions |
| Goal preservation | pass | preview evidence remains subordinate to D1–D4 and never claims dispatch, merge, release or parent completion |

Strong intent is the explicit `$split` invocation. The router false-positive is
any ordinary request to split prose/files/data. Verifiable completion requires
the exact `manifest_sha256`, accepted validation, and four zero effect counters.
Authority stops at preview; there is no dispatch flag or child adapter.

## Deterministic evidence

- `python3 skills/improve-skills/scripts/audit_skills.py --strict`
- `python3 skills/improve-skills/scripts/audit_skills.py --verdicts docs/acceptance/v2.6.5-split-skill-verdicts.json --scope split --strict`
- `python3 scripts/check-skill-budget.py`
- `python3 tests/test_split_components.py`
- `python3 scripts/split-preview.py tests/fixtures/split/preview-request.json`

The validated schema-v1 verdict is in
`docs/acceptance/v2.6.5-split-skill-verdicts.json`. Local audit success proves
the governed skill boundary only; D2–D4 remain bound to their validation,
wave/locality and join fixtures.
