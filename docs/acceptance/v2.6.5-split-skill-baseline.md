# LLM Obsidian 2.6.5: Split skill pre-authoring baseline

## Boundary

The approved Subplan D recorded the fresh-context no-skill RED before the
candidate existed: a general agent did not reliably produce a bounded natural
count, zero-effect authority stop, or complete evidence/non-goal inheritance.
The raw cases are preserved in
`tests/fixtures/split/skill-raw-baseline.json`; they contain no candidate
instructions or expected manifest.

This component task performed zero child provider calls. It preserves the
approved pre-authoring ruling instead of replaying a model after exposing the
answer, which would not be fresh-context evidence and would violate the task's
no-child-provider-effect boundary.

## RED denominator

| Case | Required behavior | Recorded no-skill failure |
|---|---|---|
| `raw.variable-count` | derive one or a natural number | hard-coded four children |
| `raw.zero-effect` | prove no dispatch/provider/worktree/surface effect | no verifiable authority boundary |
| `raw.inheritance` | exact digests, evidence, non-goals, pipeline and budget | incomplete inheritance |
| `raw.completion-authority` | bounded completion evidence and stop | plausible proposal substituted for proof |

All four failures precede `skills/split/SKILL.md`. The GREEN candidate is
explicit-only, invokes a facade with no dispatch option, and binds completion
to an exact sealed manifest plus the eight-class validation result.
