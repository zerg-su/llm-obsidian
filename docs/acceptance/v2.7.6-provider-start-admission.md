# 2.7.6 provider-start admission — candidate evidence

Scope: the bounded closure of the two terminal findings on preserved 2.7.5
negative-candidate HEAD `51a84ab93d2772b1b2edcb9ab81f3601af88ddee`:
`F275.PROVIDER_EFFECT_TOCTOU_REMAINS` and
`F275.UNRESOLVABLE_CONTRACT_FAILS_OPEN`. This candidate adds no scheduler,
polling, retry policy, lifecycle rewrite, public schema or CLI, callback
rewrite, authority owner, lock protocol, or snapshot subsystem. It claims no
review acceptance, reap, merge, push, tag, publication, installation, or
dogfood.

## E276.PROVIDER_START_RED

- `tests/harness/test_exact_head_review_attempt.py` requires the exact
  receipt/HEAD admission callback to reach the runtime owner rather than run
  only before `runtime.start`. A refused owner-side admission starts zero
  providers and leaves the exact attempt in recoverable attention; the
  unchanged control admits once immediately before every fresh start.
- `tests/harness/test_runtime_sessions.py` exercises the real
  `RuntimeSessionManager.start` boundary. Admission is consumed after surface
  and launch preparation but before `start-provider` is registered. Refusal
  closes the prepared surface, clears its resource binding, and produces no
  provider command or process handle; success is consumed exactly once before
  the provider command.
- `scripts/harness/workflows/review.py` threads the existing exact admission
  callback through the existing runtime start seam. The actual start-effect
  owner in `scripts/harness/runtime_session_launch.py` consumes it at the
  boundary above. No new lock or snapshot protocol is needed.

## E276.CONTRACT_RESOLUTION_RED

`tests/harness/test_task_review_flow_units.py` covers four distinct dispatch
contract outcomes through the production resolvers:

1. no dispatch record or a legacy record with an empty contract retains its
   existing no-admission behavior;
2. a resolved contract without a verify primitive may omit admission;
3. a resolved verify-owning contract requires the immutable durable
   VerificationAuthority receipt/HEAD admission;
4. a present nonempty contract that neither canonical resolver can compile
   fails closed before reservation or provider effect.

`scripts/task_review_flow.py` represents those outcomes with a private tagged
result. Resolution failure can no longer collapse into resolved no-verify.
Present admission for a recordless, legacy-empty, or otherwise unresolved
contract remains fail-closed.

## Exact measured baselines

- The unchanged 27-file active review-authority contour is exactly 15,603
  production lines, with zero writable authorities and zero incident
  literals. The four-line increase is the private resolution tag and its
  fail-closed branch in `scripts/task_review_flow.py` (exactly 1,442 lines).
- `scripts/harness/runtime_session_launch.py` is exactly 1,157 lines after the
  owner-side admission seam.
- The scripts ratchet remains 290 Python files and is exactly 109,280 lines,
  a measured 17-line production increase with no headroom.

## E276.CANDIDATE_GATE

Run from the exact committed candidate tree:

- `python3 tests/harness/test_exact_head_review_attempt.py`
- `python3 tests/harness/test_runtime_sessions.py`
- `python3 tests/harness/test_task_review_flow_units.py`
- `python3 tests/harness/test_review_transport.py`
- `python3 tests/harness/test_callback_submit_recovery.py`
- `python3 tests/harness/test_callback_submit_recovery_runtime.py`
- `python3 tests/harness/test_callbacks.py`
- `python3 tests/harness/test_lifecycle_crash_matrix.py`
- `make test-harness`
- `make test-model-routing`
- `make test-docs`
- `make test-code-quality`
- `python3 scripts/task_contract.py validate`
- `git diff --check`

The exact candidate HEAD and configured scoped verification results are bound
outside the commit by the Harness verification receipt and task pipeline gate
receipt, avoiding a self-referential documentation amendment after commit.
