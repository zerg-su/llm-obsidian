# LLM Obsidian 2.6.0 foundation checkpoint

The internal, non-release foundation checkpoint is:

- Git commit: `d50990dc9c76b659c82978a0c8644b9bdbd99b29`
- Local branch point: `foundation/2.6.0`
- Paired fixture-set SHA-256:
  `56f418e33d1778ec96974b1c8d26a852c7d5add02ac8590e9f9c6e7a9a1872b4`
- Verification profile: `scoped`
- Paired route: `codex` / `sol` / `high`
- Review preset: simple, at most one verification iteration

This checkpoint includes the task-metadata v4 and Outcome Contract transport,
Wiki Summary v2/reap compatibility, review telemetry, bounded reviewer
inspection, protected upstream drift evidence, RT10 command-evidence work,
skill-quality pre-audit, and the exact review-recovery repairs approved by the
current-checkout review at `6f9db232d2622ff27de8d531e53f3cf29400aae9`.

## Frozen paired evidence

The authoritative input inventory is
`evals/paired-v2.6.0/manifest.json`. It binds two baseline/post pairs:

1. fix: `clarify → debug → tdd → review`, including one deliberately green but
   goal-misaligned pre-existing test suite;
2. design: `clarify → design → prototype → review`, with production code kept
   unchanged.

For each pair, baseline and post-change plans have different lifecycle bytes
but an identical canonical Outcome Contract digest. Fixture bytes, route,
verification profile, review budget, and expected evidence IDs are immutable.
`scripts/paired-evals.py verify` fails closed on any drift; its `compare`
subcommand only compares already completed reports and does not execute or
schedule model work.

## Green foundation gate

At the exact checkpoint SHA:

- `make test` passed;
- `make paired-eval-verify` passed;
- `scripts/validate-vault.py --summary` passed;
- model-literal lint confirmed that the paired route uses the central `sol`
  alias rather than a concrete model literal;
- `git diff --check` passed.

## RT10 and skill-audit re-anchoring

The accepted RT10 live run remains applicable to this checkpoint by exact-byte
reuse rather than by an inferred compatibility claim. The RT10-owned command
capture and distillation surfaces are byte-identical between its accepted
foundation HEAD `9c8cf8b` and `d50990d`:

```text
git diff --quiet 9c8cf8b..d50990d -- hooks .claude/hooks scripts/command_evidence.py skills/distill-runbook
exit 0
```

Later foundation commits changed Outcome Contract, review, summary, reap, and
paired-evaluation plumbing, but did not change the command-capture sanitizer,
typed command-evidence ingestion, or `distill-runbook` skill exercised by RT10.
The original run therefore remains the live evidence for those unchanged
bytes; the exact-checkpoint `make test` remains the integration evidence for
their composition with the later foundation.

The pre-branch skill-quality audit was also re-anchored without rewriting its
findings. The audited skill, lint, budget, and Codex-adapter bytes are unchanged
between `d50990d` and the documentation-only release HEAD used for the rerun.
The following checks passed together:

- `python3 skills/improve-skills/scripts/audit_skills.py --strict` — 32 audited,
  0 errors, 0 warnings;
- `make test-improve-skills`;
- `make test-instruction-lint`;
- `make test-skill-budget`;
- `make test-codex-adapter` — 22 passed, 0 failed.

This re-anchoring preserves the existing nine finding identities and their
A/B/C/integration ownership while proving that all three workstreams still
branch from the exact audited bytes at `foundation/2.6.0`.

The checkpoint is an internal comparison base, not a public tag. Workstreams
A, B, and C must branch from it. Later release integration may advance
`release/2.6.0`, but it must not rewrite this branch point, paired fixture
bytes, Outcome Contract schema/serialization, or telemetry definitions.
