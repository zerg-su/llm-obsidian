# v2.6.6 RC2 active-surface baseline

The frozen subject is `3f8d02e936af99b49aaf83a466c756799c38736c`
(`v2.6.6-rc1-fix2`). All counts below are physical line counts from that exact
tree before RC2 product mutation.

| Metric | Frozen value |
|---|---:|
| `scripts/**/*.py` files | 259 |
| `scripts/**/*.py` lines | 85,431 |
| classic required-deletion files | 6 |
| classic required-deletion lines | 2,128 |
| conditional compatibility files | 1 |
| conditional compatibility lines | 89 |
| active-authority audit files | 8 |
| active-authority audit lines | 3,868 |
| active-authority writable symbols | 0 |
| strict direct runtime callers | 0 |

## Production caller/import map

`tests/test_v266_rc2_scope.py` parses every Python file under `scripts/` and
requires zero importers outside the deletion contour for each allowlisted
module. The classic modules import only one another. The conditional
`archive_task_reviews.py` wrapper has no importer. Non-import textual mentions
at the frozen subject are limited to migration lint/instruction inventory and
historical docs/tests; none is executable caller authority.

Required zero-caller deletion allowlist:

- `scripts/cmux_agent_supervisor.py`
- `scripts/cmux_supervisor_policy.py`
- `scripts/cmux_supervisor_review.py`
- `scripts/cmux_supervisor_contracts.py`
- `scripts/cmux_task_watchdog.py`
- `scripts/cmux_trust_prompt.py`

Conditional zero-caller deletion allowlist:

- `scripts/archive_task_reviews.py`

Retained active boundaries include `scripts/cmux_agent_support.py`,
`scripts/cmux_surface_lifecycle.py`, `scripts/cmux_workspace_lifecycle.py`,
`scripts/task_sessions.py`, and the adapters under `scripts/harness/adapters/`.
The RC2 deletion slice is not authorized to replace or broaden those owners.

## Reproduction

```bash
python3 tests/test_v266_rc2_scope.py
python3 scripts/runtime-harness-lint.py --strict --json
python3 scripts/code-quality-audit.py --json
```

The final RC2 evidence must repeat the same counters on exact candidate HEAD,
show a lower Python LOC/file count and a smaller classic authority surface, and
show no new provider route, scheduler, state machine, store, coordinator, or
compatibility compiler.
