# LLM Obsidian 2.6.4 callback recovery baseline

Subplan A starts from exact product tag `v2.6.3`. The frozen incident fixture is
`tests/harness/fixtures/callback-submit/v2.6.3-missing-review-submit.json`; its immutable
fixture-body SHA-256 is recorded by the Slice 0 commit.

The reproducer holds operation/run/lane/generation constant, keeps the exact
provider alive, presents an interactive idle prompt, and changes only the
content-free screen digest at the 900-second boundary. The v2.6.3 generic
liveness policy records that digest change as progress and also refuses to
nudge an interactive prompt. No `.review-input.json`, callback, or receipt is
present, so the existing deadline observer reaches `callback-timeout` with zero
provider send and zero accepted callback.

This RED names the required production change: a reviewer-callback classifier
must measure progress from exact callback-generation evidence rather than raw
screen churn, while retaining the generic one-nudge ceiling and every existing
deadline, ownership, and artifact validator.

## D-264 dispositions owned here

| Defect | Baseline disposition | Evidence |
|---|---|---|
| D-264-01 | included | frozen missing-submit fixture and runtime RED |
| D-264-02 | included | accepted-child publication Slice 4 RED |
| D-264-13 | included | exact superseded cleanup Slice 4 RED |
| D-264-14 | included | control-plane stage matrix below |

No production timing floor, model/provider budget, public PipelineSpec schema,
or release/join file changes in this baseline.
