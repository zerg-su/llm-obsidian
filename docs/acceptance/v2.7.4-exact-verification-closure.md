# 2.7.4 exact verification closure — exact candidate evidence

Scope: the bounded 2.7.4 class-level closure of the two material findings in
the terminal 2.7.3 Sol XHigh review, on the preserved combined
2.7.1 + 2.7.2 + rejected-but-useful 2.7.3 candidate base
`48934b95b787b3d8f46f7a3c9f14aa9d6308ebea`. The terminal 2.7.3 findings are
immutable problem evidence, not approval; their root is not reopened here.
This note claims no review acceptance, reap, merge, plugin installation,
dogfood, tag, or publication.

## Closed findings (terminal 2.7.3 review, immutable evidence)

- `F273.MISSING_PREDECESSOR_FAIL_OPEN`: when the deterministic attempt-0
  predecessor record was lost while attempt-1 successor or invalidation
  evidence survived, the verification owner classified the orphaned lineage
  as a fresh run — minting a second attempt-0 verifier, re-running probes,
  linking a receipt, and finishing review-side over a broken lineage.
- `F273.EXACT_HEAD_ACCEPTANCE_RACE`: a clean commit that moved the product
  HEAD after the final in-effect HEAD observation could still yield linked
  and consumed stale verification authority — through receipt persistence,
  controller link recovery, summary consumption/attention clearance, and the
  review drive — releasing review effects for a HEAD that was never
  verified.

## Repair (candidate commits on the task branch)

- `8cb0c9e5` — RED matrices: ten direct orphan census rows (missing
  attempt-0 × pending/verifying/failed/complete/receipted successor and
  valid/malformed/symlinked/conflicting/drifted invalidation evidence), a
  real-worker orphaned-lineage runtime case with a repeated-wake idempotence
  check, a truly-empty-identity-space fresh-run control, and five
  clean-commit injection windows (post-observation persistence, link
  recovery, summary consumption, dirty-tree consumption, review drive) with
  an exact clean same-HEAD control.
- `ebc0fbfe` — the GREEN. Slice A: `adopt_invalidated_verification_successor`
  consumes one read-only census (`_orphaned_predecessor_lineage`) before
  classifying a missing attempt-0 record as a fresh run. The census derives
  both bounded attempt identities with the existing constructors, reads only
  exact owner store records and exact owner/runtime paths through
  symlink-aware regular-file checks, and treats any surviving successor
  record or `receipt.json`/`response-receipt.json`/`invalidation.json` trace
  for either bounded identity as proof of a lost predecessor. Orphaned
  lineages latch exactly one typed `pipeline-verification-orphaned-lineage`
  attention with zero state transition, store creation, probe effect,
  controller link, attention clearance, or review drive; repeated wakes
  converge with zero new effects. Slice B: one cohesive current-candidate
  predicate (`_verification_candidate_is_current`: fresh `git rev-parse
  HEAD` equality plus empty `git status --porcelain`) is re-checked
  immediately before receipt linking at persistence, controller link
  recovery, summary consumption, and review launch
  (`_review_drive_candidate_is_current`, which re-enters the existing
  code-owned verification/rebind path `_current_head_verification_ready`
  instead of replaying provider effects). A receipt remains immutable
  evidence for its own exact HEAD and is never rewritten; stale or
  dirty-tree authority is never linked or consumed, and the continuation
  halts to the existing bounded rebind path or one typed
  `pipeline-verification-stale-authority` attention. Drift owned by a
  durable review-resolution notification
  (`pipeline-review-resolution-notify.json`) keeps its existing resolution
  machinery (`_review_resolution_drift_in_flight`), so resolved-head
  re-verification and summary refresh keep converging.
- The configured Sol XHigh implementation review of `cff76205` reported two
  material findings, both applied in one bounded findings patch (the final
  candidate commit). `F274.CANDIDATE_PREDICATE_TOCTOU`:
  `_verification_candidate_is_current` now brackets its tree observation
  with two exact HEAD observations (HEAD, clean tree, HEAD again), so a
  clean commit landing between any of its reads invalidates the whole
  observation instead of yielding currency for the predecessor HEAD;
  deterministic injections cover both inter-read windows plus the
  undisturbed, dirty, and empty-expectation controls.
  `F274.RESOLUTION_DRIFT_BYPASS`: the review-resolution notification is now
  identity-exact (schema, own operation, `sent` status, 64-hex packet
  digest, and reviewed HEAD equal to exactly the stale receipt HEAD) and
  strictly wait-only — it suppresses one stale-authority attention latch
  while the executor commits the resolution, but it never authorizes
  receipt linking, link recovery, summary consumption, or review release;
  same-HEAD dirt at the review drive is likewise wait-only with no rebind
  side effect. A foreign, pending, digest-less, schema-drifted,
  head-mismatched, malformed, or symlinked notification is never a
  resolution in flight. In total 234 production lines across the existing
  verification owner (165) and its two seam consumers (summary 20, review
  bridge 49); no production module, no schema/CLI change, no new lock
  protocol, no callback rewrite.

## Fail-closed matrix (hermetic, at the candidate)

- Every orphan census row — missing attempt-0 with a pending, verifying,
  failed, complete, or receipted successor, or with valid, malformed,
  symlinked, conflicting, or identity-drifted invalidation evidence — stays
  typed attention with no adoption, no binding, and byte-identical durable
  evidence (`tests/harness/test_verification_authority.py`).
- The real worker over an orphaned lineage (lost attempt-0, pending
  attempt-1, durable invalidation) latches
  `pipeline-verification-orphaned-lineage`, runs zero probes, mints no
  child, links no receipt, and a repeated wake adds zero effects
  (`tests/harness/test_runtime_task_summary.py`).
- A truly empty attempt identity space remains the only fresh-run
  classification (control, green).
- A clean commit inside the persistence window is never linked; link
  recovery after a clean commit never relinks stale authority; summary
  consumption after a clean commit or over tracked/untracked dirt refuses
  the receipt and halts to typed attention; the review drive refuses
  current-HEAD drift and launches no provider effect.
- The exact clean same-HEAD receipt keeps the ordinary consumption path with
  no attention and exactly-once semantics; the resolved-changed-HEAD
  corridor keeps converging through the existing resolution machinery,
  with the identity-exact notification suppressing only the attention
  latch, never restoring currency or authorizing consumption.
- Inherited 2.7.1–2.7.3 authority, retry, cleanup, routing, contract, and
  crash matrices stay green; the corridor simulator and custom runtime
  fixtures now repository-ignore runtime transport files exactly as
  `run_case` and the product checkout (`.git/info/exclude`) already do, so
  cleanliness observation sees only real product dirt.

## Exact commands (rerun from the candidate tree root)

- `python3 tests/harness/test_verification_authority.py`
- `python3 tests/harness/test_runtime_task_summary.py`
- `python3 tests/harness/test_review_transport.py`
- `python3 tests/harness/test_callback_submit_recovery.py`
- `python3 tests/harness/test_callback_submit_recovery_runtime.py`
- `python3 tests/harness/test_callbacks.py`
- `python3 tests/harness/test_lifecycle_crash_matrix.py`
- `make test-harness`
- `make test-model-routing`
- `python3 scripts/code-quality-audit.py --rc1-authority-json`
- `python3 tests/test_code_quality_audit.py`
- `python3 tests/harness/test_v266_rc1_integration.py`
- `make test-docs`
- `make test-code-quality`
- `python3 tests/rc4_scope_ratchet.py`
- `python3 scripts/task_contract.py validate`
- `git diff --check`

## Final active-authority contour

Measured once after production code was final, over the same separately
owned 27-file manifest (`config/active-review-authority.json`; membership
unchanged):

```json
{
  "production_loc": 15333,
  "production_file_count": 27,
  "writable_authority_count": 0,
  "incident_literal_count": 0
}
```

`config/code-quality-baseline.json`
`rc1_active_authority.final_max_production_loc` is bound to exactly `15333`
— the measurement of this candidate, not a growth allowance;
`tests/harness/test_v266_rc1_integration.py` asserts exact equality, so both
growth and stale headroom fail. The entire 165-LOC contour delta is the
read-only orphan census, the bracketed current-candidate predicate, and the
identity-exact wait-only resolution marker inside
`scripts/harness/runtime_worker_verification.py`, whose owned file-lines
hotspot moves to exactly its measured 1261 lines. The live scripts scope
ratchet moves by the measured 234-line closure cost (165 owner + 20 summary
seam + 49 review bridge) to the exact 290-file / 108,915-line candidate with
no speculative headroom.

## Complete-gate evidence bound to the exact candidate HEAD

As established by the 2.7.3 review (`F273.EXACT_HEAD_GATE`), a committed
gate receipt can only ever describe a predecessor tree. Exact-HEAD
completion evidence for this candidate is therefore recorded
owner-externally, where recording it does not move the product HEAD:

- `.task-pipeline/outputs/pass-0/candidate-gate-receipt.json` (task-pipeline
  runtime state, repository-ignored, reviewer-observable in the task
  worktree) binds one complete `make test-harness` run — exit code, output
  hash and line count, and registered suite count — to the exact resolved
  final HEAD observed clean immediately before and after the run.
- The Harness's own scoped verification receipt for this dispatch
  (`pipeline-verification/<child>/receipt.json` in the owner store) binds
  the configured verification profile to the same exact resolved HEAD
  through the schema-v2 verification authority.
- The gate is rerunnable verbatim from the candidate tree root:
  `make test-harness`.

## Residual boundaries (unchanged by design)

- Correct fail-closed rejection, dirty/unavailable HEAD, identity ambiguity,
  and exhausted retry remain typed attention; no scheduler, kernel, generic
  repair framework, polling loop, new lock protocol, compatibility path,
  public schema/CLI change, or new authority owner was added.
- Verification retry ceilings (`MAX_SAME_HEAD_ATTEMPT_INDEX = 1`) and
  approval policy are unchanged.
- Local merge, plugin update, smoke corridors, full dogfood, version
  promotion, tag, push, and publication remain coordinator-owned later
  actions.
