# LLM Obsidian 2.6 paired post-change comparison

Date: 2026-08-02

Machine-readable evidence:

- post report: `docs/acceptance/v2.6-paired-post.json`;
- comparison: `docs/acceptance/v2.6-paired-comparison.json`;
- frozen baseline: `docs/acceptance/v2.6-paired-baseline.json`.

Both post-change tasks reused the frozen fixture set, Outcome Contract digests,
Sol/high route, scoped verification profile, and one-round simple review budget.
Both reached `achieved`, established all four declared evidence items, required
no user intervention, produced no duplicate effect, and were independently
approved at verification iteration zero.

| Case | Baseline → post duration | Model rounds | Review rounds | Callback failures | Outcome |
|---|---:|---:|---:|---:|---|
| fix | 925 → 757 seconds | 4 → 4 | 1 → 1 | 1 → 0 | achieved → achieved |
| design | 1021 → 1028 seconds | 1 → 1 | 1 → 1 | 0 → 0 | achieved → achieved |

The comparison has no behavioural regression. The seven-second design duration
increase is recorded as directional wall-clock noise; duration is reported but
is not a hard regression field because provider and review latency are external
to the frozen semantic/lifecycle budgets. Completion, evidence, interventions,
round counts, callback failures, and duplicate effects did not regress.

The first post-design sequence did not pass. Its third frozen forward test
triggered the planned architecture stop, and
`docs/acceptance/v2.6-paired-design-architecture-stop.md` records the evidence-
driven removal of fixture-specific recovery policy. The single fresh validation
cycle then produced task HEAD `f5d024ee85701c81fc41b5f9b4395594d398176b`,
preserved runnable prototype source, assigned live effects to the inspected
runtime-worker owner, and received an automatic finding-free approval.
