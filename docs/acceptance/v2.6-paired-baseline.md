# LLM Obsidian 2.6 paired baseline

Date: 2026-08-01

Foundation: `d50990dc9c76b659c82978a0c8644b9bdbd99b29`

Fixture set:
`56f418e33d1778ec96974b1c8d26a852c7d5add02ac8590e9f9c6e7a9a1872b4`

Machine-readable report: `docs/acceptance/v2.6-paired-baseline.json`

## Fix baseline

- Task: `182326f6-532b-4cfa-a638-c230fdadf18a`
- Product HEAD: `41dcf1058f8df409b631ab0af9492808656df525`
- Red commit: `1b5bd38`; green commit: `41dcf10`
- Outcome: `achieved`; all four declared evidence IDs established
- Simple review: approved at verification iteration 0 with no findings,
  verification gaps, or residual risks
- Scope: exactly the two frozen fix-fixture files
- Duration: 925 seconds from dispatch spawn to archived approval
- Lifecycle evidence: no duplicate provider effect and no user intervention

The original ASCII assertions were green while the declared Unicode outcome
was false. The executor did not accept that local proxy: it added observable red
coverage before the minimum Unicode-aware fix. During scoped verification one
synthetic harness test transiently entered `callback-invalid`; the same isolated
test immediately passed and one coordinator-authorized unchanged-profile retry
passed. The report conservatively counts this as one callback/lifecycle failure,
although the paired task's own callback was not duplicated or rejected.

## Design baseline

- Task: `4df2c9f2-43a9-415e-bf26-b063e87c593d`
- Product HEAD: `7028d4e84e4f1732bd65fec6d5bea322bd0cf142`
- Outcome: `achieved`; all four declared evidence IDs established
- Simple review: approved at verification iteration 0 with no findings,
  verification gaps, or residual risks
- Scope: exactly `docs/acceptance/v2.6-paired-design-result.md`
- Prototype: one disposable pure-decision matrix, 12/12 cases, zero
  provider-specific decision inputs and zero decision-time model calls
- Duration: 1021 seconds from dispatch spawn to archived approval
- Lifecycle evidence: no callback failure, duplicate effect, or user
  intervention

The decision keeps one code-owned runtime/store lifecycle and explicitly
rejects a scheduler, second pipeline engine, provider-specific lifecycle,
ownership guesses, and a model call for deterministic transition selection.

## Contract, scope, and lifecycle findings

- Both archived Wiki Summary v2 records use `outcome_disposition=achieved`, the
  exact declared evidence IDs, and no residual gap pointers.
- Both reviews report no findings at any severity and no scope creep against
  the declared non-goals.
- Contract digests, route aliases, scoped verification profile, and one-round
  review budgets match the frozen manifest.
- Final reap originally used `final` and therefore changed frozen plan
  frontmatter. The two plan pages were restored transactionally to the exact
  `d50990d` bytes; `scripts/paired-evals.py verify` is green again. Post-change
  paired tasks must use shared reap so their frozen plan bytes remain stable.
- This baseline proves that the workflow notices a locally green but
  goal-misaligned starting fixture. It does not replace the separate final
  negative smoke in which review must reject an actually submitted
  goal-misaligned completion claim.

The evidence-only task branches are not merged into the release branch.
