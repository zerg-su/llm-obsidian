# LLM Obsidian 2.6.2 release-readiness ledger

Date: 2026-08-03

This is the bounded evidence entry point for the final 2.6.2 review. The
review packet, not this document, binds the exact final Git HEAD; the ledger
does not attempt an impossible self-reference to the commit that contains it.

## Model-free release gates

The executor ran the following sequence successfully on clean candidate
`fdde3230ac1ac2b355143844ca8df3c62f5e5788` before this evidence-only
follow-up:

```bash
make test
make acceptance-check
python3 scripts/validate-vault.py --summary
python3 scripts/codex-adapter.py --check
scripts/mcp-gateway/mcp-gateway.sh codex-sync --check
git diff --check
```

The run exited `0`; `make acceptance-check` reported four valid harness cells
at that candidate, the adapter and gateway reported no changes, and the vault
validator was clean. The only subsequent product edits represented by this
ledger are the regenerated plans index, direct README progress guidance, and
this evidence page. The complete sequence must be rerun after that commit and
before the final review. A read-only reviewer can independently reproduce the
full suite by checking the packet HEAD out into its owned writable scratch and
running the same commands there.

## Bounded live cmux transition

The code-owned `harness.status_segment` publisher was exercised against exact
workspace `6F3DE542-E296-454B-80A8-EED18B5BEC01` while final Deep review owner
`af2b55b1-e5be-4023-88ba-5c01fc9d6e46` had two live provider parents and two
live review-round children:

```text
rendered=0/4 · 4▶ 4⌛
published=True
```

After both callbacks were accepted and the four exact review operations were
closed through `harness-cli cancel` plus `reconcile`, the same publisher used
one fresh bounded cmux inventory snapshot for the same workspace and owner:

```text
rendered=''
published=True
```

The second publication invoked workspace-scoped `clear-progress`. Historical
attention records with missing exact surfaces did not contribute to either
projection, and no operation state was changed by status collection or
publication.

## Review findings resolved by this follow-up

- `status.missing-surface-attention`: fixed in `8c514f0` with a red/green
  regression covering attention plus a known missing exact surface;
- `scope.hot-thread-eviction`: the separately user-approved cache repair is
  now explicit in the Outcome Contract and has its dedicated vault-writer
  regression;
- `stale-plans-folder-index`: `scripts/reindex.py --folder-indexes` regenerated
  the canonical listing and now includes c-000098 among 18 plan pages;
- `readme-progress-not-explained`: both READMEs now state current-workspace,
  live-step, idle-clear, and non-backlog semantics directly;
- `release-ready-evidence-missing`: this ledger records the completed bounded
  live transition and gives the exact reproducible model-free gate.

The minor parser-cohesion observation remains intentionally deferred: unknown
cmux inventory preserves the existing bar instead of falsely clearing it, as
required by the approved fail-safe contract. A broad parser refactor is not a
2.6.2 patch-release requirement.
