# LLM Obsidian v2.4.1 dogfood acceptance

Date: 2026-07-31

This report records the live compiled-pipeline acceptance window required by
`docs/releases/v2.4.1.md`. Durable runtime evidence lives in the pinned
coordinator checkout at
`/Users/zak/Projects/worktrees/llm-obsidian-2-4-1-dogfood-coordinator`.
Temporary dogfood targets are not release product code.

## Completed task matrix

| Task | Executor | Pipeline | Completion | Review | Durable result |
|---|---|---|---|---|---|
| `df241-change-clamp` | Claude Sonnet, low | `engineering/change` | attention | simple, Codex Terra | `[[2.4.1 dogfood change clamp]]` |
| `df241-change-parse-bool` | Claude Sonnet, low | `engineering/change` | attention | simple, Codex Terra | `[[2.4.1 dogfood change parse bool]]` |
| `df241-change-slugify` | Codex Terra, low | `engineering/change` | attention | simple, Claude Opus | `[[2.4.1 dogfood change slugify]]` |
| `df241-change-task-count` | Codex Terra, low | `engineering/change` | attention | simple, Claude Opus | `[[2.4.1 dogfood change task count]]` |
| `df241-fix-retry-bound` | Codex Terra, low | `engineering/fix` | attention, two attempts | simple, Claude Opus | `[[2.4.1 dogfood controlled provider restart]]` |
| `df241-change-stable-unique` | Codex Terra, low | `engineering/change` | attention | deep | `[[2.4.1 dogfood deep change stable unique]]` |
| `df241-fix-label-spaces` | Claude Sonnet, low | `engineering/fix` | attention | simple, Codex Terra | `[[2.4.1 dogfood fix label spaces]]` |
| `df241-fix-redact-lower` | Claude Sonnet, low | `engineering/fix` | attention | simple, Codex Terra | `[[2.4.1 dogfood fix lowercase redaction]]` |
| `df241-fix-runtime-default` | Codex Terra, low | `engineering/fix` | attention | simple, Claude Opus | `[[2.4.1 dogfood fix runtime default]]` |
| `df241-fix-progress-zero-v2` | Claude Sonnet, low | `engineering/fix` | autonomous, three-pass ceiling | deep, Codex Terra override | `[[2.4.1 dogfood autonomous retry deep review v2]]` |
| `df241-controlled-provider-restart-v2` | Codex Terra, low | `engineering/fix` | attention | simple, same-model Terra | `[[2.4.1 dogfood verified provider restart v2]]` |

All eleven parent operations are `complete`, have
`accepted_callback_kind=wiki-summary`, and were filed through coordinator-owned
reap. The matrix covers both executor runtimes, both engineering profiles,
simple and deep review, attention and autonomous completion policies, and
same-model plus cross-model review routes.

## Required exceptional paths

### Bounded verification retry

`df241-fix-retry-bound` completed with parent `attempt=2`. Its accepted
reproduction evidence was retained while the fix pipeline performed the bounded
retry; the final parent operation is `complete`.

### Controlled provider restart

The first restart attempt is excluded from acceptance: it created only a
fixture-side `requested` marker and did not increment the harness counter.

The replacement task `df241-controlled-provider-restart-v2` used the existing
identity-bound guardian control port after the accepted root-cause receipt.
Durable evidence records:

- parent `model_restarts=1` with limit `2`;
- `pipeline-fix/provider-restart-1.json` status `restarted`;
- old PGID `34146` / new PGID `37281`;
- distinct old/new process identities;
- resume at the first missing phase, followed by final HEAD verification,
  approved review, Wiki Summary callback, and reap.

### Deliberate typed escalation

`df241-escalate-unreproducible` produced a durable `pipeline-decision` after an
accepted `cannot-reproduce` receipt. Its only allowed decisions were `stop` and
`retry-with-fixture`; the coordinator selected `stop`. The parent and derived
verification operation are now terminal `cancelled` with no owned resources.

## Recovery observations

The dogfood window exposed and repaired repository-owned lifecycle defects:

- review callback submission no longer depends on shell redirection;
- repaired review gates can resume their bound parent session;
- explicit resume rearms expired callback waits without duplicating lanes;
- exact host-side verification can satisfy a managed-sandbox Unix-socket
  boundary without weakening the task profile;
- attention-policy retry exhaustion no longer writes a misleading
  `callback-error.json` before the coordinator decision;
- a resolved verification escalation can be resumed through the public harness
  CLI without replaying accepted phases.

Historical callback-error receipts from failed/recovered test runs remain as
accounting evidence. A full store scan after cleanup found zero nonterminal
operations. No callback error is attached to an unresolved operation, and no
owned task/reviewer surface remains pending cleanup.

## Acceptance verdict

The live dogfood requirements are satisfied:

- 11 completed compiled tasks (requirement: at least 10);
- Claude and Codex executors;
- `engineering/change` and `engineering/fix`;
- simple and deep review;
- bounded retry;
- one proven controlled provider restart;
- one deliberate typed escalation;
- zero nonterminal operations after lifecycle cleanup;
- no unresolved callback failure or auto-close miss.

Release readiness still requires the model-free commands from
`docs/releases/v2.4.1.md` and final release review against the cleaned release
HEAD.
