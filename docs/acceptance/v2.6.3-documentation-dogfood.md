# LLM Obsidian 2.6.3: documentation dogfood

Date: 2026-08-04. Frozen handbook HEAD:
`390a3953ffe0bfb4114e263bfdc81ee80b48af2b`. No network fetch, provider launch,
push, tag, publish, deploy, or release occurred.

## Walkthrough 1 — clean checkout to first durable result

Disposable local clone:
`/tmp/llm-obsidian-2.6.3-dogfood.k8GfS3/repo`.

1. `bash bin/setup-clean-machine.sh --check --skip-proxy --skip-docling`
   detected macOS CLT and Python, printed the vault/gateway/adapter actions and
   required manual secret step, and performed no install.
2. A first `vault-write.py` transaction created `Dogfood First Result`, log and
   hot entries with test address `c-999999`. The transaction succeeded, but
   `validate-vault.py --summary` correctly rejected it because the address
   counter was only 99.
3. Recovery used `scripts/allocate-address.sh`, `vault-write.py --sha256`, and
   one optimistic update transaction. It replaced the invalid address with
   allocator-owned `c-000099` and removed the stale hot entry.
4. `scripts/reindex.py --quiet` and `validate-vault.py --summary` passed.
5. `retrieve.py "Dogfood First Result" --top 5 --json` returned the new page at
   sparse rank 1 with score `44.97089583648603`. Dense cache was absent and the
   response honestly reported `mode: sparse`, `degraded: true`.

Ruling: E1 established. The failure/recovery proves that the page and handbook
must tell users to allocate an address, not invent one; whole-vault validation
is independent of a successful writer transaction.

## Walkthrough 2 — plan to PipelineSpec, review and reap readiness

The same disposable clone ran public model-free seams:

```text
make test-docs
python3 tests/harness/test_custom_pipelines.py
python3 tests/test_dispatch_runner.py
python3 tests/harness/test_workflows.py
python3 tests/test_reap_runner.py
```

Observed results:

- 23 handbook pages, 34 skills and the documentation PipelineSpec passed.
- The example compiled through the strict custom contract using existing
  `tdd`/`review` skills and checks; unknown skill/check, authority expansion,
  graph bombs and built-in-equivalent proposals remained rejected.
- Dispatch tests bound plan/Outcome Contract, exact route/worktree, custom
  approval challenge, review preset and compiled hash before effects.
- Workflow tests established review lane identity, finding aggregation,
  same-session verification, safe research isolation and resumable reap.
- Reap tests established exact plan/review/result identity and one final vault
  transaction; invalid preflight could not consume the Wiki Summary callback.

Ruling: E3 and the lifecycle portion of E2 established at model-free public
seams. This walkthrough does not claim that a provider review or final reap was
executed; those remain harness-owned terminal stages.

## Walkthrough 3 — maintainer quality change to release dry run

The candidate-skill decision was used as the maintainer change: a proposed
skill and 8,000-byte registry cap were tested, found redundant, and removed.
The dirty first-result clone ran:

```text
make test-skill-budget test-improve-skills test-codex-adapter
python3 scripts/release-acceptance.py check
```

Skill budget, improve-skills and adapter suites passed. Release acceptance then
correctly rejected the clone because the dogfood page and regenerated
`.vault-meta` files made HEAD behaviorally dirty. The failure was not bypassed.

A second clean clone of the same HEAD,
`/tmp/llm-obsidian-2.6.3-release-dogfood.XeAE9n/repo`, ran:

```text
python3 scripts/release-acceptance.py --root <clean-clone> check
```

Result: `release-acceptance: 4 harness cells valid at
390a3953ffe0bfb4114e263bfdc81ee80b48af2b`.

Ruling: E4 maintainer route established. The two-clone comparison proves that
release evidence binds a clean exact HEAD and cannot reuse a green local gate
from a dirty walkthrough.

## Post-amendment bounded re-walk

After the handbook gained the manual parallel-task guide and the lifecycle
command examples were corrected, the affected final page set was walked again:

```text
make test-docs
python3 scripts/harness-cli.py inspect --help
python3 tests/harness/test_task_review_mechanism_recovery.py
make test-harness-coverage
```

The docs gate reported 24 reachable pages and 34 skills, compiled the published
PipelineSpec, validated every relative link and required guide section, and
rejected the mutation fixture that omits `<operation-id>`. The CLI help confirmed
that `inspect`, `resume`, `cancel`, and `close` require the positional exact
operation identity. The new guide is therefore checked as an executable
contract route rather than inferred from its page count. Exact final-HEAD
identity remains the job of the content-free release receipt after commit.

## Residual boundary

These are local/model-free walkthroughs. Exact release-candidate evidence must
be rerun after metadata/changelog commits, full `make test`, and harness review.
The coordinator remains the only reap owner.
