# LLM Obsidian 2.6.5: live bounded Split dogfood

Date: 2026-08-05. The single successful live activation was frozen from Join
candidate `9d09899e98f1c74c946586fba6a979b112b26f5e` with manifest
`542845a2f2609a9d8b841ae49ef879c45828eb9bbee8c6c31a0319bb8e39c629`.
It dispatched one dependency-free child named `whole-plan` on Codex/Sol
(`gpt-5.6-sol`, high effort), with `review_policy.mode=skip`, a 200000-token
ceiling and an 1800-second ceiling. The child owned only its acceptance page.

## Immutable identities

| Item | Exact evidence |
|---|---|
| Fresh request | `d2650000-0000-4000-8000-000000000015` |
| Parent | `/private/tmp/llm-obsidian-265-split-dogfood-2-019fd214` |
| Child | `/private/tmp/llm-obsidian-265-split-dogfood-child-2-019fd214` |
| Child branch and commit | `task/llm-obsidian-265-split-dogfood-2` at `9ae578ad2e63cb447324b01718f4b0754089fa63` |
| Workspace and surface | `C6982461-D829-443D-9106-895D2D57E0F6`; `2E347764-82B8-4F39-B46C-066C0573D10A` |
| Harness run | `75eeb326-7aeb-579e-9250-0bcec912859d` |
| Accepted child summary | SHA-256 `a81238db4683294d3a72f23da667ddffdd79e789132196fc6522333e1b696a01` |
| Skipped-review receipt | SHA-256 `b3929844a6f29173ba6235c4b5428f92ac26bef349b85868d63130956386d633` |
| Reaped vault commit | `643fcdbff557d86969daec3b07c004df9c14314f` |

The immediately preceding request ending in `0014` was consumed by a
fail-closed Codex authentication-output adapter rejection. It created no
provider, model, cmux or Split effect and was never retried. The exact-marker
adapter repair was committed before the fresh request above.

## Result and recovery boundary

The child validated its frozen task contract, inspected all assigned E1-E14
evidence, ran the focused Split activation, Outcome-contract, schema and
dispatch tests, and committed only its owned acceptance page. Its typed summary
callback was accepted with the exact hash above. Review was intentionally
skipped by the frozen policy, so this activation consumed one Codex/Sol model
effect and no reviewer or independent-provider effect.

Reap first stopped before a vault commit because the display title did not
identify the exact context-path stem. The code-owned dispatch renderer and
strict exact-binding repair planner were fixed with fail-closed regressions.
The preserved transaction then wrote one result page/address/log/hot entry and
committed it exactly once at `643fcdbff557d86969daec3b07c004df9c14314f`.
No page, address or log entry was duplicated.

The completion receipt became durable before the pending `reap-finalize`
effect was settled. The code-owned reconciler validated the exact task, owner,
operation, run, callback, prepared receipt, result page/address, cardinalities
and committed vault HEAD, then resolved only that existing effect. Ordinary
`harness-cli reconcile`/`close` reached revision 20, state `complete`, with no
owned process, surface, workspace or other live resource. Neither reap nor the
vault transaction was replayed.

The preserved launch and terminal receipts subsequently produced
`ready-to-join` with every effect counter at zero. Deterministic Join returned
the one manifest-order integration entry at child commit
`9ae578ad2e63cb447324b01718f4b0754089fa63` and proved E1-E14 without merging
or repeating a child/provider/model effect.

## Independent-provider disposition

The registered capability source supplied a typed, freshness-bounded
`AvailabilityEvidence` record with status `unknown`. Finalization therefore
selected only `finalization-primary` with reason `availability-unknown` and
performed zero provider calls while compiling the route decision. No second
provider was probed.

This dogfood performed no push, tag, publish, install, deploy or release
effect.
