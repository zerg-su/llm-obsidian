# LLM Obsidian 2.6.5 lifecycle simulator readiness

## Supplementary candidate

This simulator is a post-Join, zero-real-effect verification layer. Its
immutable base admission is
[`v2.6.5-lifecycle-simulator-base.json`](v2.6.5-lifecycle-simulator-base.json).
The admitted first-parent chain is Join `09fd1c31e35c4f9b16fdbfcf17eb933360a0b374`
plus the authorized provider-editor bootstrap
`cca8c98c95596229c607ccf5e565e9688696858e`.

The implementation and gate code is frozen at product HEAD
`45d1c8d97b7f18d2562980476d8239fe92a65cdd`, tree
`2d053c869bc0170bf574769669c89ec364efd2ae`. Later documentation or automatic
review-resolution commits do not rebind that admission receipt.

| Outcome evidence | Bound proof |
|---|---|
| `SIM-E1-world` | `LifecycleWorld` uses production `OperationStore`, state machine, callback broker, delivery reducer, liveness controller, worker poll/exit seams, cleanup ledger, and reconciliation; only provider/process/cmux/event/time adapters are fake. |
| `SIM-E2-scheduler` | Stable seeded topological schedules, bounded enumeration, trace SHA-256 identity, and exact `--scenario/--seed/--trace` replay. |
| `SIM-E3-crash-restart` | Named post-durability failpoints cover operation transition, liveness publication, error latch, effect reserve/resolve, and cleanup receipt; restart reconstructs volatile adapters from durable bytes and preserves one provider input identity. |
| `SIM-E4-invariants` | Independent oracle checks operation/liveness coherence, effect and callback uniqueness, exact identity, terminal monotonicity, latch retirement, atomic artifacts, cleanup receipt, and terminal resource freedom. Mutation probes make the gate RED. |
| `SIM-E5-regression-corpus` | Seven content-free historical declarations are linked from `v2.6.5-causal-ledger.md`; every initial state fails by its declared invariant, its current path is GREEN, and two passes have identical fixture and trace identities. |
| `SIM-E6-gates` | Fast and extended Make targets, suite registration, adapter vocabulary conformance, honest harness coverage, full suite, release acceptance, vault validation, and clean-status checks are green. |

## Deterministic gate results

Two consecutive fast executions produced the same summary:

```json
{"actions":33,"invariants":7,"scenarios":7,"schedules":7,"wall_seconds":0.0}
```

The explicit bounded extended sweep produced:

```json
{"actions":61,"invariants":7,"scenarios":8,"schedules":11,"wall_seconds":0.0}
```

Exact replay example:

```bash
python3 tests/harness/lifecycle_gate.py \
  --scenario extended-pairwise-lifecycle-boundaries \
  --seed 26599 \
  --trace 782861ea9a7a91c5bc0a7d0c7861da2753af86e461415c0e09916fe9f48b60fd
```

That trace terminates `complete` with provider, model, cmux, and network real
effect counters all zero.

## Verification

The product candidate passed:

```bash
make test-lifecycle-simulator
make test-lifecycle-simulator-extended
make test-harness
make test-harness-coverage
make test-model-routing
make test
python3 scripts/release-acceptance.py check
python3 scripts/validate-vault.py --summary
git diff --check
git status --short
```

Harness statement coverage was 76.04% (`17,562/23,096`) across 153 modules,
with all critical floors green and 4,370 deterministic transition-matrix
cases. The simulator adds no public runtime schema, dependency, store, journal,
or production randomization switch.

## Boundary

The simulator proves deterministic logical composition and crash-prefix
recovery against fake external adapters. It does not replace live provider,
cmux, process-signal, filesystem/power-loss, or network dogfood. Push, tag,
publish, deploy, and release remain separately authorized actions and are not
performed here.
