# LLM Obsidian 2.6.5 lifecycle simulator readiness

## Supplementary candidate

This simulator is a post-Join, zero-real-effect verification layer. Its
immutable base admission is
[`v2.6.5-lifecycle-simulator-base.json`](v2.6.5-lifecycle-simulator-base.json).
The admitted first-parent chain is Join `09fd1c31e35c4f9b16fdbfcf17eb933360a0b374`
plus the authorized provider-editor bootstrap
`cca8c98c95596229c607ccf5e565e9688696858e`.

The implementation and gate code is frozen at product HEAD
`bfb726e77e666daaece694187bc10323984c01e0`, tree
`2e61c13afd6bb6f811598972e56e6b4fbb4b3b6a`. The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-bfb726e/receipt.json)
reconstructed and verified that exact HEAD. Later documentation or automatic
review-resolution commits do not rebind that admission receipt.

## Finalization mechanism addendum

The historical simulator admission above remains frozen at `bfb726e`; none of
its gate counts, traces, coverage figures, or claims are rewritten here. The
subsequent coordinator-authorized stale-review quarantine mechanism is frozen
at product HEAD `5c97baed512ad6a9b2fd855438bb4a78f9819a4e`, tree
`1014395942d3f1acbd93958a9823d82f13ca634a`. It adds one typed, fail-closed
transition for an exact retained deep-review attempt whose accepted callback
receipt and callback artifact identities provably differ. The transition
archives the original bytes and identities, rejects ambiguous or matching
callbacks and incomplete ownership evidence before effects, terminalizes only
the bound stale ownership through lifecycle APIs, and permits one zero-fix-budget
fresh Codex/Sol review boundary without ingesting, reconstructing, rebinding,
replaying, or reinterpreting either retained callback.

The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-5c97bae/receipt.json)
reconstructed and verified exact HEAD `5c97bae` with all 15 commands green:
the full suite, 75.97% honest harness coverage (`17,953/23,632`) across 156
modules, 4,370 transition-matrix cases, release and vault validation, quality,
skill, instruction, adapter and MCP gates, resource-free harness status,
doctor `ok`, diff check, and clean status. The earlier `bfb726e` receipt remains
immutable and independently valid.

## Stale OS-ownership recovery addendum

The historical `bfb726e` simulator admission and `5c97bae` callback-drift
quarantine evidence above remain unchanged. The later coordinator-authorized
signal-free stale-ownership recovery is frozen at clean product HEAD
`1f2c2e801b89a6f7d80a3e083060b4bfc3320eaa`, tree
`bdb5891146cd0def6e8e5069449f57c1bf138458`. Its code-owned lifecycle path
requires the archived callback-drift evidence and exact gate/progress identity,
reconstructs durable process-group, process, supervisor, surface, workspace,
and product-root ownership without sending a signal, rejects any exact live
match, identity reuse, ambiguity, partial evidence, unrelated ownership, or
archive tampering, and atomically clears resources only after two matching
read-only absence proofs. Crash/restart progress preserves retirement-receipt
digests so an already completed cleanup cannot be repeated.

The coordinator adopted `e3db9bb9de8243e3615de85921d093f724d1ac69` as the
narrow typed `product_root` cleanup-binding repair. Descendant `1f2c2e8` adds
only the matching value to the pre-existing CLI cleanup fixture; it does not
weaken production ownership validation. Diagnostic-only attempts `d21605`,
`d21606`, and `e3db01` are not verification evidence. The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-1f2c2e8/receipt.json)
reconstructed exact HEAD `1f2c2e8` with all 15 commands green: full suite,
76.02% honest harness coverage (`18,104/23,814`) across 156 modules, 4,370
transition-matrix cases, release and vault validation, quality, skill,
instruction, adapter and MCP gates, resource-free harness status, doctor
`ok`, diff check, and clean status. No retained callback or provider effect was
replayed, and this addendum does not claim that the pending stale ownership has
been retired or that a fresh review has started.

## Supported-close compatibility addendum

The historical simulator, callback-drift quarantine, and signal-free ownership
evidence above remain immutable. The later coordinator-authorized
supported-close compatibility mechanism is frozen at clean product HEAD
`3493c348693d43efc7bc59848844c56e8f4cbecb`, tree
`8d5318537a3988119867688eb8195adfe7c28056`. It consumes only the exact retained
parent operations
`b63552c9-60c2-54b3-99c9-093525a65c44-engineering-eef68b02` and
`b63552c9-60c2-54b3-99c9-093525a65c44-intent-a917e21d` after the supported
harness close path has left each terminal `cancelled`, resource-free, without a
pending effect, and with a succeeded `request-exit` receipt. The mechanism
validates both parents and their accepted retained round children before the
first mutation, records immutable zero-effect receipts, and atomically projects
each exact child terminal through the existing lifecycle state machine. It
rejects live, ambiguous, partial, non-terminal, receipt-mismatched,
archive-drifted, or unrelated state and cannot ingest, reconstruct, rebind,
replay, or reinterpret either retained callback.

Focused lifecycle, crash/restart, idempotency, rejection, zero-signal, and
zero-callback/provider-replay regressions are included. The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-3493c34/receipt.json)
reconstructed exact HEAD `3493c34` with all 15 commands green: full suite,
76.04% honest harness coverage (`18,232/23,978`) across 157 modules, 4,370
transition-matrix cases, release and vault validation, quality, skill,
instruction, adapter and MCP gates, resource-free harness status, doctor
`ok`, diff check, and clean status. The receipt itself performs no retained
review recovery and starts no provider or callback effect; supported recovery
and the single authorized fresh review remain harness-owned lifecycle steps.

## Fresh-boundary identity addendum

The supported-close archive and its immutable receipts remain unchanged. Clean
product HEAD `5d4ee642ff7b1de0d5d1189194a80e296728fefc`, tree
`c7c3fe70ca68715022accb158e35b894a821a61d`, fixes the fail-closed boundary
discovered after terminal quarantine: a fresh-review authorization now binds
the exact active review operation identity, while retaining the dispatch
operation identity as separately validated provenance. Version-one internal
authorization receipts remain replay-compatible; new receipts use the typed
version-two identity pair and reject unrelated dispatch provenance. The change
does not repeat cleanup, consume retained callbacks, or expand the fresh-review
budget.

The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-5d4ee64/receipt.json)
reconstructed exact HEAD `5d4ee64` with all 15 commands green: full suite,
76.04% honest harness coverage (`18,234/23,979`) across 157 modules, 4,370
transition-matrix cases, release and vault validation, quality, skill,
instruction, adapter and MCP gates, resource-free harness status, doctor
`ok`, diff check, and clean status. Attempt
`26500000-0000-4000-8000-0000005d4ee7` is diagnostic-only: its sole failure was
a non-reproducing stop-hook wall-clock p95 reading of 1.021 seconds, and it
published no evidence. The successful receipt starts no reviewer or provider.

## Post-verification review-drive continuation addendum

All earlier simulator, quarantine, stale-ownership, supported-close, and
fresh-boundary evidence remains immutable. Clean product HEAD
`ed711cb2ff5f231487566a9457f4b8dd152c38aa`, tree
`08fdeabe83250e818b5d9806199c797c8a78d072`, adds the narrow
coordinator-authorized continuation for the exact live dispatch provider after
scoped verification completed but its fresh review drive failed. The internal
transition binds the active review and dispatch identities, completed
verification receipt and output, failed pending marker, quarantine archive,
terminal retained operations, provider events, exact process/supervisor/surface
ownership, and current clean descendant before its first effect. It rejects
dead, reused, partial, unrelated, or ambiguous ownership and any marker,
receipt, gate, archive, progress, callback, or Git drift.

The write-ahead continuation resumes prepared or split publication after a
crash, starts the already-authorized fresh review at most once, carries the
completed scoped-verification receipt forward without rerunning verification,
and writes a distinct started marker so the surviving worker cannot drive the
same review twice. Focused tests cover exact identity and live ownership,
prepared and mid-publication crashes, restart, idempotency, zero signal,
zero callback/provider replay, and the supported `resume` facade. The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-ed711cb/receipt.json)
reconstructed exact HEAD `ed711cb` with all 15 commands green: full suite,
76.08% honest harness coverage (`18,667/24,536`), 4,370 transition-matrix
cases, release and vault validation, quality, skill, instruction, adapter and
MCP gates, resource-free harness status, doctor `ok`, diff check, and clean
status. The receipt itself performs no retained cleanup, verification replay,
review/provider launch, callback ingestion, or harness resume.

## Darwin exact-live ownership addendum

All earlier simulator and lifecycle-recovery evidence remains immutable. Clean
product HEAD `2c3260625840def62e5a1a05b180050f71ff4570`, tree
`7ab9e57af9652a41f0fd6a49e85c20bb6ea25e67`, adds the narrow
coordinator-authorized read-only Darwin ownership proof for the exact live
dispatch provider. Ordinary successful zero-signal probes and all non-Darwin
behavior are unchanged. Only when both Darwin zero-signal probes are denied
with `EPERM` or `EACCES` does the adapter consult `proc_bsdinfo`; it reports the
provider/supervisor pair alive only when the persisted process identities and
process group still match, both processes are running and non-zombie, and the
provider's parent is the exact persisted supervisor. Missing proof, PID/PGID or
supervisor reuse, wrong parentage, stopped/zombie/exited/absent state, and
non-permission probe errors remain fail-closed.

Focused adapter and post-verification lifecycle regressions prove the exact
live case, every required rejection, non-Darwin compatibility, crash/restart
and idempotent continuation, zero signals, zero callback/provider replay, and
at-most-one fresh review. The coordinator decision compiler preserves the
historical quarantine, supported-close, active-review, and dispatch provenance
while binding a recovery receipt to the newest exact-live authorization. The
immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-2c32606/receipt.json)
reconstructed exact HEAD `2c32606` with all 15 commands green: full suite,
76.14% honest harness coverage (`18,731/24,601`) across 159 modules, 4,370
transition-matrix cases, release and vault validation, quality, skill,
instruction, adapter and MCP gates, resource-free harness status, doctor
`ok`, diff check, and clean status. The receipt performs no lifecycle recovery,
provider/reviewer launch, callback ingestion, process signal, or cmux mutation.

## Post-fresh publication synchronization addendum

All earlier simulator, quarantine, supported-close, and Darwin ownership
evidence remains immutable. Clean product HEAD
`0fe0985aada45c1f87aafae1db612f822d63c82f`, tree
`889405b4ec5d2d5ccb2a86900efb48c352f8306a`, adds the narrow
coordinator-authorized recovery for a fresh review whose two Codex/Sol lanes
were already created with succeeded `start-provider` effects but whose gate,
quarantine progress, final marker, and dispatch callback-waiting projection
were only partially published.

The recovery compiles the exact newest coordinator decision over the consumed
post-verification authorization, validates the prepared continuation receipt,
fresh gate and authorization, archived and retained evidence, exact parent and
round records, reviewer runtime artifacts, generation-2 provider delivery, and
zero replay counters, then uses crash-idempotent compare-and-swap publications.
It never starts, replaces, or resumes a provider; probes or signals a process;
touches a cmux surface; repeats verification or quarantine cleanup; or ingests
or reinterprets a callback. Focused tests cover both publication crash windows,
restart convergence, idempotency, exact identity and artifact drift rejection,
at-most-one fresh review, and zero process/cmux/provider/callback effects. The
supported `reconcile` facade suppresses its normal status-surface publication
only for the one synchronization transaction. The prepared review target may
differ from the mechanism checkout only when Git proves the latter is a clean
descendant; that exact repair HEAD and tree are sealed into the durable
synchronization receipt and must remain unchanged through restart.

The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-0fe0985/receipt.json)
reconstructed exact HEAD `0fe0985` with all 15 commands green: full suite,
76.02% honest harness coverage (`19,020/25,019`) across 162 modules, all
critical floors, 4,370 transition-matrix cases, release and vault validation,
quality, skill, instruction, adapter and MCP gates, resource-free harness
status, doctor `ok`, diff check, and clean status. The receipt itself performs
no retained lifecycle recovery or provider/reviewer/callback effect.

## Fresh child-round lifecycle-progress addendum

The earlier post-fresh synchronization evidence and every quarantine, retained
callback, supported-close, verification, and provider receipt remain
unchanged. Clean product HEAD
`1c693a28d8717f706417e94a875ee05e88e6c775`, tree
`3413bffd2fd91c3dc9148fd8cf581c01c6cc60b4`, adds the narrow
coordinator-authorized compatibility boundary for the same exact fresh review
rounds after their callbacks advance monotonically beyond `awaiting-callback`.
The synchronizer accepts only `verifying`, `finalizing`, or resource-free
`complete` rounds with exact parent/lane/run/route identity, unchanged parent
resources and succeeded `start-provider` effects, a matching generation-2
callback receipt, and a typed contiguous provider-event and delivery cursor.
It rejects callback or provider drift, unsupported or regressed states,
non-empty round resources, effects, ambiguous operations, and non-monotonic
prepared-crash recovery before publication.

Focused regressions cover every accepted successor, terminal resource freedom,
prepared-crash advancement, explicit state regression, callback-receipt drift,
unchanged parent/round/provider bytes, idempotency, and zero signal,
callback/provider replay, or review-start counters. The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-1c693a2/receipt.json)
reconstructed exact HEAD `1c693a2` with all 15 commands green: full suite,
76.04% honest harness coverage (`19,069/25,078`) across 162 modules, every
critical floor, 4,370 transition-matrix cases, release and vault validation,
quality, skill, instruction, adapter and MCP gates, resource-free harness
status, doctor `ok`, diff check, and clean status. The receipt performs no
lifecycle synchronization, callback/provider replay, reviewer launch, process
signal, or cmux mutation.

## Fresh-progress authorization and timing-gate addendum

All historical quarantine, retained callback, supported-close, verification,
fresh-review, and provider evidence above remains immutable. Clean product HEAD
`5afd1841373eecbf38672d1b209a34fd3f20d7ce`, tree
`7dcd9effadd116f35dd68c083e466a3a6e68d8be`, contains two bounded
coordinator-authorized mechanism repairs. First, the authorization compiler
accepts only the exact ordered chain from the prior post-fresh-publication
grant through the fresh child-round progress grant to its latest compatibility
resolution; missing, reordered, ambiguous, broadened, or scope-drifted chains
fail closed. Second, the Stop-hook timing test retains the strict `1.000s` SLO
but replaces a heterogeneous functional-test p95 with one deterministic
warm-up and seven monotonic measurements. It tolerates only one isolated high
scheduler-jitter sample; two samples at or above the SLO fail, and a regression
with an injected real delay proves the gate remains mutation-sensitive.

Focused compiler regressions preserve the original continuation identity while
rejecting every unauthorized chain form. Focused timing regressions cover the
warm-up boundary, one-outlier tolerance, strict threshold, malformed samples,
and sustained real delay; the full Stop-hook integration passes 67 checks. The
immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-5afd184/receipt.json)
binds exact HEAD `5afd184` with all 15 commands green: full suite, 76.05% honest
harness coverage (`19,098/25,113`) across 162 modules, every critical floor,
4,370 transition-matrix cases, release and vault validation, quality, skill,
instruction, adapter and MCP gates, resource-free local harness status, doctor
`ok`, diff check, and clean status. The exact live lifecycle reconcile remained
unused while this evidence was generated; the receipt contains no callback or
provider replay, reviewer launch, process signal, or cmux mutation.

## Exact authorization-chain addendum

Clean product HEAD `98a258ca42c36e0ea30b3e957f7413f43510b6d5`, tree
`bef57ffda1080156ef8e1ada3b8699a33c85133f`, extends that compiler only for
the coordinator-named immutable record tail ending at
`resolution-b28b1e20822edf26a9c4ffa399abf305`. It verifies every exact record
identity, digest, and predecessor, rejects duplicate or unrelated resolution
forms, and preserves the original post-fresh continuation. A separate
read-only boundary proof requires the unchanged fresh gate digest, a clean Git
descendant of the already-passed `5afd184` release subject, the committed
`26500000-0000-4000-8000-000005afd184` receipt, and absence of the
post-fresh synchronization receipt that would indicate the one reconcile had
already been consumed.

The immutable
[`release-final` receipt](evidence/v2.6.5/lifecycle-simulator-98a258c/receipt.json)
binds exact HEAD `98a258c` with all 15 commands green: full suite, 76.04%
harness coverage (`19,169/25,208`) across 163 modules, every critical floor,
4,370 transition-matrix cases, release and vault validation, zero-blocker
quality, skill, instruction, adapter and MCP gates, resource-free local harness
status, doctor `ok`, diff check, and clean status. Receipt generation did not
consume the reconcile or cause any callback/provider replay, reviewer launch,
process signal, or cmux mutation.

| Outcome evidence | Bound proof |
|---|---|
| `SIM-E1-world` | `LifecycleWorld` uses production `OperationStore`, state machine, callback broker, delivery reducer, liveness controller, worker poll/exit seams, cleanup ledger, and reconciliation; only provider/process/cmux/event/time adapters are fake, and their external-world facts are durably independent of worker restart. |
| `SIM-E2-scheduler` | Stable seeded topological schedules, bounded enumeration, source-fixture SHA-256 binding, trace SHA-256 identity, and exact `--scenario/--seed/--trace` replay. |
| `SIM-E3-crash-restart` | Named post-durability failpoints cover operation transition, liveness publication, error latch, effect reserve/resolve, and cleanup receipt; restart executes the production worker poll/liveness path from durable bytes, preserves one provider input identity, and cannot resurrect a fake resource recorded dead or missing. |
| `SIM-E4-invariants` | Independent oracle checks operation/liveness coherence, effect and callback uniqueness, exact identity, terminal monotonicity, latch retirement, atomic artifacts, cleanup receipt, and terminal resource freedom. Mutation probes make the gate RED. |
| `SIM-E5-regression-corpus` | Seven content-free historical declarations are linked from `v2.6.5-causal-ledger.md`; every initial state fails by its declared invariant, its current path is GREEN, admitted source fixtures are digest-bound, semantic forbidden-effect declarations are mutation-sensitive, and the timeout/result collision covers both legal race orientations. |
| `SIM-E6-gates` | Fast and extended Make targets, suite registration, adapter vocabulary conformance, honest harness coverage, full suite, release acceptance, vault validation, and clean-status checks are green in exact-head immutable receipt attempt `26500000-0000-4000-8000-000000bfb726`. |

## Deterministic gate results

Two consecutive fast executions produced the same logical summary, with
independently measured wall times:

```json
{"actions":25,"invariants":7,"scenarios":7,"schedules":8,"wall_seconds":0.584482}
{"actions":25,"invariants":7,"scenarios":7,"schedules":8,"wall_seconds":0.555536}
```

The explicit bounded extended sweep produced:

```json
{"actions":39,"invariants":7,"scenarios":8,"schedules":10,"wall_seconds":0.680067}
```

Exact replay example:

```bash
python3 tests/harness/lifecycle_gate.py \
  --scenario extended-pairwise-lifecycle-boundaries \
  --seed 26599 \
  --trace bf3aed91d7b1d53a0421c7e4466af267de6c84e324f26770a49193234968b158
```

That trace terminates `complete` with provider, model, cmux, and network real
effect counters all zero.

## Verification

The product candidate passed:

```bash
make test-lifecycle-simulator
make test-lifecycle-simulator-extended
make test-harness
make test-harness-coverage
make test-model-routing
make test
python3 scripts/release-acceptance.py check
python3 scripts/validate-vault.py --summary
git diff --check
git status --short
```

Harness statement coverage was 75.87% (`17,671/23,290`) across 153 modules,
with all critical floors green and 4,370 deterministic transition-matrix
cases. The immutable receipt records all 15 `release-final` commands, their output
digests, the exact subject HEAD and tree, and a resource-free harness. Its clean
reconstruction also exposed a stale retrieval-quality-marker edge in the Stop
hook; `bfb726e` contains the narrow regression-tested repair. The simulator adds
no public runtime schema, dependency, store, journal, or production
randomization switch.

## Boundary

The simulator proves deterministic logical composition and crash-prefix
recovery against fake external adapters. It does not replace live provider,
cmux, process-signal, filesystem/power-loss, or network dogfood. Push, tag,
publish, deploy, and release remain separately authorized actions and are not
performed here.
