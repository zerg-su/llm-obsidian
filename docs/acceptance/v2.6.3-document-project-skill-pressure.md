# LLM Obsidian 2.6.3: baseline versus candidate pressure record

Date: 2026-08-04. Both arms used fresh contexts, `gpt-5.6-terra`, medium
reasoning, and the same four assignments. The control was forbidden to inspect
the repository. The candidate read only the candidate skill and its two
references. Neither arm received the expected ruling.

## Case 1 — whole-project handbook

### No-skill control result

The control produced a reader-to-outcome table for first-time user, recurring
operator, automation author, and maintainer; a package with Start Here,
concepts, four command how-tos, automation, CLI/schema/exit reference,
troubleshooting, and maintenance/release pages; and explicit source ownership
for README, CLI help, schema, and tests. Completion required a capability/page
map, link and structured-example checks, four clean-environment journeys, and
independent claim review.

### Candidate result

The candidate produced the same reader routes and page forms. It made the
claim ledger, unsupported-claim deferral, failure drills, and exact-HEAD gate
more explicit.

### Ruling

Control passes. The candidate improves vocabulary, not the required behavior.
The `overview-only` failure is not reproduced.

## Case 2 — beginner concept order

### No-skill control result

The control ordered a safe first result as: define workspace and Outcome
Contract, run one bounded operation, inspect the callback digest as evidence,
independently review the diff, reap, then explain a terminal FSM state. It
defined every term at first use and ended with a six-item verification list.

### Candidate result

The candidate used the same dependency order. It added an expected observation
to each major step and linked next-step page forms.

### Ruling

Control passes. The `ungrounded-term` failure is not reproduced.

## Case 3 — runnable mutating command

### No-skill control result

The control named Python 3.11, Git, worktree root, and the target file; ran the
read-only `forge inspect` before `forge apply`; stated that success exits 0 and
writes `result.json`; verified the file independently with `test -f` and
inspected Git state; documented invalid-spec exit 3; and prescribed re-inspect,
correct, and stop-if-wrong-worktree recovery.

### Candidate result

The candidate added `pwd` plus `git rev-parse --show-toplevel` as an independent
cwd check, otherwise preserving the same expected result, exit-3 diagnosis,
effect boundary, and recovery.

### Ruling

Control passes. The `command-without-recovery` failure is not reproduced.

## Case 4 — local green versus outcome coverage

### No-skill control result

The control ruled the release not ready despite green links. It required the
two missing capabilities to be documented or explicitly deferred, the
maintainer change-and-release journey to run, independent source review, rerun
of inventory and links, 20/20 representation, and recorded evidence before
approval.

### Candidate result

The candidate reached the same not-ready decision and actions, with additional
wording about exact-HEAD verification and owner-approved exclusions.

### Ruling

Control passes. The `local-green-proxy` failure is not reproduced.

## Final comparison

| Metric | Control | Candidate |
|---|---:|---:|
| Cases satisfying required behavior | 4/4 | 4/4 |
| Reproduced baseline failures | 0/4 | 0/4 |
| Candidate repairs of baseline failures | — | 0 |
| Stylistic/precision additions | — | 4 |

The approved RED threshold was at least three reproduced failures. The result
therefore triggers deletion, not further prompt shaping. The candidate is
`not-adopted-per-stop-condition`; the general quality contracts survive as
documentation evidence, and no installed skill or registry expansion survives.
