# LLM Obsidian 2.6.6 RC4 skill-quality evidence

## Live engineering-discipline aggregate

The registered `scripts/engineering-eval-runner.py` adapter was executed once
from the coordinator-owned Codex 0.147.0 execution boundary with the frozen
`terra` / `medium` / 240-second profile. The two earlier managed-child
transport attempts and the later child network-allowlist attempt produced zero
accepted case receipts and zero completed model results; they are retained in
the typed escalation chain and are not counted as behavioral executions.

The successful coordinator invocation ran each frozen case exactly once and
returned the following complete schema-valid receipt:

The independently inspectable provenance bundle is
[`evidence/v2.6.6/rc4-engineering-discipline-live.json`](evidence/v2.6.6/rc4-engineering-discipline-live.json),
SHA-256 `6df616c7f71b86b4a074ad013cabd29311f1eed9213ef08ced04651fed47d986`.
It binds the original product HEAD, runner bytes, registered profile, distinct
command-log and execution working directories, authorization/adoption/amendment
chain, exact typed receipt, and byte-equivalent prompt/schema digests without
replaying the aggregate.

```json
{"cases": [{"failures": [], "id": "rc4.ambiguity", "status": "pass"}, {"failures": [], "id": "rc4.overengineering", "status": "pass"}, {"failures": [], "id": "rc4.unrelated-edits", "status": "pass"}, {"failures": [], "id": "rc4.missing-regression-proof", "status": "pass"}], "effort": "medium", "model": "gpt-5.6-terra", "scenario_manifest_sha256": "07ab7a4b15e3c47627aa46e1ecd48aaac1c18ffb00216db4362170682290f3dc", "schema_version": 1, "source_sha256": {"AGENTS.md": "ada22d9cb058b16e55ebf8ee19c330a697b21ad1728b548b158a7459a01a34f3", "CLAUDE.md": "2f8aa275adc95fb1d575a96d659adcd082e80308d32b669904fcfdbedf731600"}, "summary": {"failed": 0, "passed": 4, "total": 4}, "type": "rc4-engineering-discipline-aggregate"}
```

Mechanical disposition: `RC4-E8-agent-discipline-and-skill-quality` has its
required live aggregate evidence. All four cases passed; no assertion,
scenario identity, provider route, model alias, or effort was substituted.

## Skill-quality disposition

- The scoped improve-skills verdict is `fix` for Split invocation metadata and
  `pass` for hierarchy, steering, pruning, and goal preservation.
- The four-byte `Fan.` description became one concise strong-intent
  description. `disable-model-invocation: true`,
  `policy.allow_implicit_invocation: false`, tools, scripts, schemas, preview
  authority, `--dispatch` authorization, waves, join, and lifecycle semantics
  remain unchanged.
- `AGENTS.md` and `CLAUDE.md` carry one behavior-equivalent engineering
  discipline contract. The lint requires one exact positive contract on each
  surface and rejects negation, optionality, duplication, or weakening.
- The four hermetic cases are identity-exhaustive and mutation-sensitive. Their
  complete canonical manifest, governing source digests, model, effort, and
  timeout are pinned independently and checked before provider execution;
  expected assertions and fixture results remain outside every model prompt.
- The base Codex `skill-creator` contract remains authoritative for names,
  descriptions, and its five standard frontmatter keys. Its literal helper
  requires unavailable PyYAML and intentionally rejects the protected Claude
  `disable-model-invocation` extension, so the repository `improve-skills`
  audit implements that base structural contract plus exactly the protected
  extension. Its dependency-free fail-closed YAML subset decodes supported
  quoted text, rejects unsupported escapes, tags, anchors, comments, malformed
  colon scalars, and every recognized implicit typed scalar before applying
  the base name/description checks. It also rejects duplicate, malformed, and
  quoted/dotted unknown keys and still requires Claude/Codex invocation parity.
  No dependency or weakened extension was introduced to manufacture a pass.

## Managed execution-routing defect

The managed child incorrectly attempted a coordinator-owned registered adapter
command. Two nested Codex app-server EPERM attempts and the later child
network-allowlist rejection produced zero accepted case receipts, zero
completed model results, and no typed case output. Escalations
`9d524650-0414-49e4-968d-c59c967d3873`,
`666eb30f-b02c-41ee-9fdd-388a855a0471`, and
`1927f41d-cda8-451e-8779-9a3b20c0d2c5` preserve those failures; coordinator
decision `05c8b9ac-49a6-4328-b716-e707f6d419c4` classifies the boundary defect.

RC4 integration regression coverage must keep coordinator-owned live execution
in the coordinator context and let a managed child consume only the durable
typed receipt. This record does not authorize another aggregate, provider,
fallback, permission, or retry path.

The transport failure record is evidence only: it exposes no replacement or
replay authorization, and only the exact single-line output-free EPERM
signature is classified as pre-model. Mixed diagnostics and typed output fail
closed as ambiguous.

## Focused verification

- `make test-agent-evals`
- `make test-instruction-lint`
- `make test-skill-budget`
- `make test-improve-skills`
- `make test-router`
- `make test-codex-adapter`
- `git diff --check`

All commands passed before the successful live invocation.
