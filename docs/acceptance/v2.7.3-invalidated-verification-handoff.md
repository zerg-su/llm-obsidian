# 2.7.3 invalidated-verification handoff — exact candidate evidence

Scope: the bounded 2.7.3 class-level repair on the preserved combined
2.7.1 + 2.7.2 candidate base `7b0b1d18f6732b329bfca15bcf82464d536ac25f`. One
proven verification-recovery regression is repaired: a verification attempt
whose probes completed but whose verification authority became invalid — the
clean product HEAD moved before callback acceptance, or the receipt was lost
to a crash — now hands off to exactly one predecessor-bound fresh attempt at
the current clean HEAD. This note claims no review acceptance, reap, merge,
plugin installation, dogfood, tag, or publication.

## Preserved incident and regression origin

The live 2.7.2 dispatch (`b65cc83b-8f4f-4b09-85a4-e8070669e10e`, preserved
untouched as negative evidence) durably recorded the sequence:

- Scoped probes finished green at HEAD A (`428936c85bea…`, 23:08) with a
  complete schema-v2 receipt linked in `pipeline-step-verify.json`.
- An evidence/doc commit moved the clean product HEAD to B (`7b0b1d18…`,
  23:32) before the summary callback was accepted.
- The HEAD-B attempt-0 verifier died mid own-identity effect: pending effect,
  partial evidence, no `receipt.json`. The root latched
  `attention-required`/`callback-invalid` with the `wiki-summary-invalid`
  error latch (23:35).
- Coordinator settlement resolved the stale effect (`succeeded`) and
  terminalized the child (00:05); one root resume still created no fresh
  verifier and re-latched `callback-invalid`.

Root cause: every recovery route in the verification owner keyed off a durable
**failed receipt**. A settled succeeded own-identity effect without a receipt
had no owner: `run_verification` raised fail-closed
(`pipeline verification state is invalid` for the terminal shape,
`pipeline verification produced no receipt` for the settled-but-verifying
shape), the summary seam converted each raise into the resumable
`wiki-summary-invalid` latch, and every code-owned resume re-entered the same
dead end — a permanent livelock. Both post-settlement shapes were reproduced
hermetically; settlement alone already crossed into the dead zone because
`supervisor.effect` rightly refuses to blind-rerun a succeeded effect.

## Repair (candidate commits on the task branch)

- `a46ac741` — RED regression matrix: the exact live ordering through the real
  runtime worker (terminal and settled-non-terminal stale attempts, repeated
  wake, crash re-entry on an interrupted successor, exhausted successor
  identities), plus adjacent attempt-1 identity/authority pins and the public
  same-HEAD path's exact-HEAD refusal.
- `ea05c193` — the fix: `RuntimeWorkerVerificationMixin` gains
  `invalidated_verification_attempt()` (classifies exactly: record exists, no
  receipt, no pending effect, own-identity effect `succeeded`, state in
  `{verifying, attention-required, failed}`) and
  `adopt_invalidated_verification_successor()` (terminalizes a non-terminal
  stale child, persists one immutable
  `pipeline-verification/<stale>/invalidation.json` binding the stale attempt
  to its successor at the current HEAD, rebinds through the existing
  `_bind_verification_attempt(attempt_index + 1)` identity constructors, and
  latches `pipeline-verification-retry-exhausted` typed attention when the
  bounded successor identity space — `MAX_SAME_HEAD_ATTEMPT_INDEX = 1` — is
  exhausted). The summary seam invokes the owner right after binding; the
  changed-HEAD review-resolution gate adopts the same handoff before launching
  verification. 82 insertions, no production module, no schema/CLI change, no
  callback rewrite, no `.task-verification.json` requirement.

## Fail-closed matrix (hermetic, at the candidate)

- Stale settled terminal attempt at the current HEAD: one wake creates exactly
  one attempt-1 successor, runs all scoped probes, persists a complete
  exact-current-HEAD receipt, links it, and finishes through the ordinary
  summary path to an accepted callback.
- Stale settled attempt still `verifying`: same handoff; the owner itself
  terminalizes the stale child first.
- Repeated worker wake after the handoff: no duplicate verifier, no second
  probe effect.
- Crash re-entry with the successor mid own-identity effect: restart resumes
  exactly that successor; no third attempt.
- Both same-HEAD identities settled terminal without receipts: typed
  `retry-exhausted` attention, no replacement, no probes, no receipt.
- Pending own-identity effects (resumable window), foreign effects, unsettled
  effects, receipt-bearing attempts, tampered receipts, and boolean/foreign
  authority mutations keep their pre-existing owners and fail-closed paths.
- Stale evidence can never authorize a different HEAD at the single authority
  ingress, and the public same-HEAD mechanism-flake path refuses a moved HEAD
  and leaves no response artifact.
- The unmodified incident reproduction driver heals on the initial wake: the
  successor verifies HEAD B and root reaches `finalizing` with no
  `callback-error.json` latch ever published.

## Exact commands (rerun from the candidate tree root)

- `python3 tests/harness/test_runtime_task_summary.py`
- `python3 tests/harness/test_verification_authority.py`
- `python3 tests/test_pipeline_verification_resubmit.py`
- `python3 tests/harness/test_callback_submit_recovery.py`
- `python3 tests/harness/test_callback_submit_recovery_runtime.py`
- `python3 tests/harness/test_callbacks.py`
- `python3 tests/harness/test_lifecycle_crash_matrix.py`
- `make test-harness`
- `make test-model-routing`
- `python3 scripts/code-quality-audit.py --rc1-authority-json`
- `python3 tests/test_code_quality_audit.py`
- `python3 tests/harness/test_v266_rc1_integration.py`
- `make test-docs`
- `make test-code-quality`
- `python3 tests/rc4_scope_ratchet.py`
- `python3 scripts/task_contract.py validate`
- `git diff --check`

## Final active-authority contour

Measured once after production code was final, over the same separately owned
27-file manifest (`config/active-review-authority.json`; membership
unchanged):

```json
{
  "production_loc": 15123,
  "production_file_count": 27,
  "writable_authority_count": 0,
  "incident_literal_count": 0
}
```

`config/code-quality-baseline.json`
`rc1_active_authority.final_max_production_loc` is bound to exactly `15123` —
the measurement of this candidate, not a growth allowance;
`tests/harness/test_v266_rc1_integration.py` asserts exact equality, so both
growth and stale headroom fail. The entire 72-LOC contour delta is the
code-owned invalidated-attempt successor inside
`scripts/harness/runtime_worker_verification.py`, which becomes an owned
file-lines hotspot at exactly its measured 1051 lines. The live scripts scope
ratchet moves by the measured 78-line repair cost (72 owner + 4 summary seam +
2 review bridge) to the exact 290-file / 108,636-line candidate with no
speculative headroom.

## Complete-gate receipt on the exact candidate tree

The complete `make test-harness` gate ran on the exact final candidate tree
and exited 0 with all 96 registered suite files.

- Receipt: `docs/acceptance/v2.7.3-harness-gate-receipt.json` — binds the
  gate to `git write-tree` `d8954dd91cdc9df4db18febb7a0728400ec06604`. The
  resolved HEAD tree differs from it by exactly three gate-inert receipt
  artifacts written afterward: the captured output log, the receipt file, and
  this section. Verify with
  `git diff --stat d8954dd91cdc9df4db18febb7a0728400ec06604 <resolved-head>`
  — no other path may appear. No registered gate reads these artifacts.
- Captured output: `docs/acceptance/v2.7.3-harness-gate-output.log`
  (3191 lines, sha256
  `885e5fa3c32c319f6e1a743a3ac4eb49f34c75269a63970ddb686e7676c34d55`).
- The gate is rerunnable verbatim from the candidate tree root.
- The candidate tree is clean at the resolved HEAD: task pipeline transport
  (`.task-pipeline/`, `.task-*.json`) is repository-ignored runtime state and
  is never staged.
