# LLM Obsidian 2.6.4 release-readiness ledger

Date: 2026-08-04.

This tracked ledger defines the release-candidate denominator. It does not
claim that its containing commit is already the terminal reviewed SHA. The
final harness review binds exact HEAD after all product, test, documentation and
version commits. Any later commit invalidates that review and requires a new
exact-HEAD boundary.

## Outcome evidence

| ID | Disposition | Evidence |
|---|---|---|
| E1-missing-submit-red | established | exact v2.6.3 commit `99c4658562e868c9659c6722631f21d1228fa37a` snapshot fixture in `test_callback_submit_recovery_runtime.py` reaches callback-timeout with zero submit |
| E2-generation-classifier | established | pure classifier matrix covers identity, ownership, prompt class, stable artifacts and deadline |
| E3-bounded-submit-recovery | established | write-ahead reservation, one fixed prompt, shared nudge ceiling and next-stage runtime fixture |
| E4-artifact-and-race-safety | established | stable input/callback/receipt fast paths, full accepted/duplicate receipt identity, stale pre-Enter activity rejection, and hard-crash tests after paste/Enter with zero replay |
| E5-terminal-fail-closed | established | terminal, stale, malformed, symlink, lost/dead/unknown and exhausted cases have zero speculative effects |
| E6-unattended-dogfood | established; final exact-HEAD review required | exact old/new runtime incident; fake-provider writes one typed artifact only after one harness Enter, then the public review-gate and task-summary entrypoints perform callback collection, resource-free parent/child cleanup and actual `reap-ready`; the tracked effect trace contains every provider, wake, gate, cleanup and summary-publication effect and no fixture-side lifecycle transition |
| E7-wikilink-self-heal | established | unique title/H1 Stop repair, strict revalidation and ambiguous/concurrent zero-write controls; durable command receipt in `v2.6.4-integration-command-evidence.json` |
| E8-durable-decisions | established | append-only chain, pointer marker, legacy backfill and digest-bound amendment tests |
| E9-reviewer-tools | established | exact OID packet/commands, abbreviation rejection and anchored DCG matrix covering shell wrapper, pipe, redirect, newline and substitution; durable command receipt in `v2.6.4-integration-command-evidence.json` |
| E10-defect-ledger | established pending terminal release audit | authoritative plan plus append-only release amendments contain D-264-01 through D-264-60 with owners, typed dispositions and evidence; the separate purpose=release review supplies the required no-loss audit |
| E11-no-regression | exact-HEAD gates required | full test, coverage, vault, adapter/MCP, snapshots and acceptance are captured in `.vault-meta/release-evidence/<exact-head>/implementation-gates.json`; final implementation and release review bind the same HEAD |
| E12-plan-review-lifecycle | established | zero-provider invalid boundaries, HEAD-byte checks for plan and explicit protected inputs, and retained design-only exact-delta verification |
| E13-superseded-review-cleanup | established | exact-owned cleanup/reconcile, active/unknown zero-close and replay tests |
| E14-harness-control-plane | established | historical baseline is retained, while `v2.6.4-harness-control-plane-final.json` and the durable E14 command receipt bind the approved PipelineSpec, all lifecycle stages, the E6 terminal receipt, zero manual ingress and zero model/prose transition authority |

## Exact release invariants

- Harness is the only lifecycle owner. Model output affects state only through
  a validated typed artifact.
- Callback submit recovery consumes the existing generic `nudge_count`; it has
  no second model-call or submit budget.
- Provider, review, verification, nudge, restart, deadline and permission
  ceilings are not broadened from 2.6.3.
- A cmux transport success is not provider acknowledgement. Continuation
  success requires typed output or an observed exact-session state change.
- No prompt body, screen body, finding body, command body or error text is added
  to content-free telemetry.
- Plan review is purpose-safe and full canonical Git OIDs remain mandatory.
- Wiki repair uses the existing parser and sole transactional writer; ambiguity
  has zero mutation.
- PipelineSpec schema and public review topology are unchanged.

## D-264 disposition audit

The authoritative ledger is the plan's `Capability Dispositions and Defect
Ledger`. D-264-01 through D-264-09 and D-264-11 through D-264-16 are included or
already shipped with executable evidence. D-264-10 is an ad-hoc reviewer probe,
not a reproducible repository contract defect. No entry is dropped or silently
deferred.

The retained historical D-264-16 session was used only for an exact ack-only
recovery attempt. Its surface disappeared before acknowledgement while the
provider process identity remained ambiguous, so the new code correctly wrote
`continuation-submit-unconfirmed` attention and did not repaste or retry again.
This is preserved negative dogfood evidence, not claimed as successful E6.
Positive E6 is supplied by the deterministic runtime incident: the accepted
callback wake re-enters `ReviewGateController`, reaches complete resource-free
parent/child cleanup, and the production task-summary observer reaches actual
`reap-ready`. The former direct-helper tail remains only a unit regression and
is explicitly excluded from release evidence. The final fresh exact-HEAD review
remains the independent audit rather than part of the dogfood fixture itself.

The user-requested 2.7 Project Spaces plan is intentional planning-only vault
content carried with the release. No 2.7 scheduler, orchestration behavior,
public DSL, or runtime implementation is part of the 2.6.4 product delta.

## Final review amendment ledger

Later exact-HEAD reviews added findings after the frozen plan ledger. They are
retained here and in the authoritative append-only plan amendment rather than
silently rewriting historical review evidence:

| ID | Owner | Finding | Disposition | Evidence |
|---|---|---|---|---|
| D-264-17 | harness | stale provider activity could acknowledge a newly pasted continuation before Enter | `included` | continuation ordering matrix requires current input visibility, one Enter and only then activity |
| D-264-18 | harness | a hard crash after callback-submit reservation could replay an ambiguous paste/Enter | `included` | reserved replay is typed `callback-submit-effect-uncertain`; kill-point tests cover after-paste and after-Enter |
| D-264-19 | harness | plan-review compilation could bind dirty plan/protected bytes to unchanged HEAD | `included` | plan and explicit artifact bytes are compared with exact `git show HEAD:<path>` before provider start |
| D-264-20 | permission policy | exact escalation allow could be followed by a shell pipe or redirect | `included` | DCG matrix and durable E9 receipt block composition and wrappers |
| D-264-21 | coordinator | 2.7 Project Spaces plan and three TaskGraph/project-task backlog entries appeared in the release diff | `included` | accepted scope deviation in `v2.6.4-accepted-deviations.json` binds the exact plan path, `wiki/backlog.md`, and the three explicit entry identities; all are user-requested planning-only vault artifacts |
| D-264-22 | review gate | resolution resume reread changed verification evidence against the original boundary digest | `included` | exact-resolution rebind reads reviewed Git bytes and binds the separate resolved delta |
| D-264-23 | callback watchdog | accepted callback target was classified as stale while parent waited for resolution | `included` | generation-bound accepted receipt ends recovery with zero provider effect |
| D-264-24 | continuation transport | stale same-heading editor could identify the current paste | `included` | pre-send screen/editor baseline is durable and required before Enter |
| D-264-25 | review gate | resumed resolution parent in `running` could not rearm | `included` | running is accepted only with durable non-empty awaiting-resolution evidence |
| D-264-26 | continuation transport | transport replay could submit stale editor contents | `included` | replay requires durable pre-send screen/editor digests; missing/unchanged baseline fails closed |
| D-264-27 | callback watchdog | duplicate broker acceptance receipt was not liveness evidence | `included` | duplicate receipt binds generation, operation, run, callback and payload digest |
| D-264-28 | release evidence | v2.6.3 annotated tag object was recorded as a commit | `included` | fixtures bind the dereferenced full commit OID `99c4658562e868c9659c6722631f21d1228fa37a` |
| D-264-29 | release evidence | E7/E9 integration executions lacked durable receipts | `included` | `v2.6.4-integration-command-evidence.json` records commands, outcomes, output digests and required controls |
| D-264-30 | release evidence | post-plan defects lacked owner fields | `included` | this table and the authoritative plan amendment bind owner, disposition and evidence for every late entry |
| D-264-31 | coordinator | completed 2.6.3 vault history appeared in the cumulative candidate delta | `included` | exact retained and derived paths in `v2.6.4-accepted-deviations.json`; no 2.6.4 runtime behavior |
| D-264-32 | callback watchdog | accepted receipts bypassed the full current broker identity | `included` | accepted and duplicate statuses share run/callback/payload validation plus accepted-child cross-check |
| D-264-33 | continuation transport | crash after paste and before transport receipt could paste twice | `included` | durable `paste-reserved` state and kill-point replay perform zero second paste |
| D-264-34 | test harness | orphan verification responder expired under full-suite load | `included` | bounded packet wait, explicit thread join and timeout assertion remove the cleanup race |
| D-264-35 | callback wake | partial wake delivery could replay a second provider-facing coordinator turn | `included` | write-ahead phases, exact-operation lock, fail-closed uncertainty, kill-points and concurrent reconcile |
| D-264-36 | continuation transport | missing screen after the first Enter could consume retry budget and send a second Enter | `included` | first/later missing-screen cases prove one Enter and typed unconfirmed attention |
| D-264-37 | release evidence | E6 stopped at child `finalizing` without terminal cleanup or following pipeline boundary | `included` | tracked receipt binds complete resource-free parent/child and `reap-ready` |
| D-264-38 | release evidence | historical E14 baseline had no final integrated authority trace | `included` | final trace artifact plus deterministic control-plane test |
| D-264-39 | test harness | task-summary helper threads raced worker cleanup under full-suite load | `included` | ready/done handshakes, bounded eventual reads, explicit joins and five repeated standalone passes |
| D-264-40 | callback watchdog | a sent recovery binding survived its accepted callback and forced the next legitimate retained-session generation into stale-generation attention | `included` | exact accepted predecessor receipt retires only the generation binding, preserves shared nudge/restart budgets, and the runtime regression accepts N+1 with zero second prompt or Enter |
| D-264-41 | vault writer | deterministic wikilink self-heal planned a valid update for writer-owned `wiki/log.md`, but the sole writer rejected its own repair payload | `included` | writer accepts only the exact current `stop-hook-link-repair` payload derived by the canonical planner; forged or ordinary log/hot page updates remain rejected |
| D-264-42 | continuation transport | Enter could be sent before its durable reservation, so a crash could replay the same generation submit | `included` | `submit-reserved`/`submit-retry-reserved` are persisted before Enter; crash replay performs zero second key and either observes exact activity or fails closed |
| D-264-43 | callback watchdog | accepted-generation retirement was not bound to the complete immutable generation identity or idempotent under concurrent workers | `included` | receipts bind operation/run/lane/generation/target and counterpart digests; concurrent exact retirement converges while mismatches fail closed |
| D-264-44 | callback watchdog | an equal durable attention marker suppressed retry of a previously failed OperationStore transition | `included` | marker replay re-reads authoritative operation state and retries the exact attention transition; a fail-once store regression proves eventual transition without another provider effect |
| D-264-45 | release evidence | E6/E14 tail was assembled by direct test transitions and hard-coded manual-effect counters | `included` | dogfood now uses production review acceptance, exit/cleanup and pipeline advancement owners; effect-derived assertions prove typed provider output plus zero manual lifecycle effects |
| D-264-46 | callback watchdog | a crash after persisting the accepted callback-submit receipt but before clearing the sent liveness state made exact retirement replay reject its own identity and stranded the next generation | `included` | deterministic kill-point leaves the exact mixed `accepted receipt + sent state` phase; replay clears only the matching binding/status, preserves shared budgets, and foreign identity remains fail-closed |
| D-264-47 | review context | a large resolution-bound plan reached `_bounded_input()` after the module extraction, but its atomic pointer writer was no longer imported and raised `NameError` before verification launch | `included` | resolution bundle imports the existing sole atomic byte writer; a 65,537-byte regression materializes and validates the exact pointer bytes/digest before any provider effect |
| D-264-48 | provider prompt policy | both unattended Sol reviewers stopped at Codex's native rate-limit model-switch dialog because the unknown-choice policy had no safe answer | `included` | only the exact dialog with option 1 selected receives `down` plus `Enter`, preserving the bound Sol route and future reminders; changed or unknown choices receive zero input |
| D-264-49 | release evidence | a documentation transaction replaced four UTF-8 characters inside one frozen Outcome non-goal and changed its canonical digest | `included` | restore the exact reviewed word `применять`; canonical Outcome SHA-256 again equals `af24873e06669632c5c45e9818a8646918e3a086a358285d1781a1a5540660ae`, and the rejected continuation had zero provider effect |
| D-264-50 | test harness | the summary-only review fixture used untracked two-second responder threads that could expire under stdlib trace before the fifth launch call | `included` | both helpers use the bounded ten-second eventual window, are joined before assertions, and must be stopped; standalone task-summary and honest harness coverage are green |
| D-264-51 | release scope | the release review found three user-requested 2.7 TaskGraph/project-task entries in `wiki/backlog.md` outside the exact D-264-21 deviation paths | `included` | D-264-21 now binds `wiki/backlog.md` plus all three stable entry identities; the entries remain planning-only and add no 2.7 runtime, scheduler, DSL, routing, or review-topology behavior |
| D-264-52 | callback watchdog | a crash after durable `state=sent` but before `receipt=sent` left an exact mixed phase that restart did not reconcile and later acceptance could not retire | `included` | restart idempotently advances only the exact sent receipt before classification with zero provider effect; the kill-point and runtime tests prove healing, preserved budgets, and malformed-receipt typed attention |
| D-264-53 | continuation transport | the bounded continuation Enter retry reserved liveness against the continuation effect identifier instead of the current callback-target digest, so worker polling saw a foreign generation and could raise stale-generation attention | `included` | retry reservation now uses the same bounded callback-target SHA-256 and complete generation identity as worker liveness; an integrated crash/replay regression reaches `awaiting-callback`, confirms provider activity, sends zero duplicate prompt/Enter, and preserves the exact binding as `sent` |
| D-264-54 | escalation records | a newly fsynced immutable decision file could be followed by a durable latest-pointer while the records directory entry itself had not been fsynced | `included` | first-use records-directory creation is synced in the worktree, every immutable record entry is synced in its parent directory before pointer publication, and the ordered durability regression proves both barriers precede the pointer |
| D-264-55 | test harness | honest coverage measured the unreserved `mark_callback_submit_uncertain` fail-closed branch below the standing liveness floor | `included` | direct behavioral assertion proves rejection without mutation; final coverage is 76.04% across 128 modules, liveness is 86.1% against an unchanged 86.0% floor, and all 4,370 transition cases remain complete |
| D-264-56 | review context | exact resolution rebound verification evidence to the reviewed commit but compared the amended current plan with the frozen reviewed-plan digest | `included` | original plan and Outcome semantics are loaded from the exact reviewed Git object during resolution; the resolved plan remains visible with the fix delta, while ordinary stale or dirty plan boundaries still fail before provider effect |
| D-264-57 | review context | the frozen implementation receipt is intentionally ignored and has no Git blob at the reviewed HEAD | `included` | tracked artifacts remain Git-backed; an ignored artifact is accepted only from its confined regular path with the exact frozen SHA-256, while missing, symlinked, escaped or changed bytes fail closed |
| D-264-58 | continuation delivery | a crash after `submit-retried` publication but before the exact generation binding reached `sent` could strand an advanced receipt beside a reserved liveness effect | `included` | `sent` is durable before `submit-retried`; a late kill-point regression proves replay promotes the same reservation without duplicate prompt/Enter and retains the next generation |
| D-264-59 | review context | the ignored-evidence fallback caught every reviewed-artifact error and could replace a stale tracked Git blob with matching mutable worktree bytes | `included` | reviewed-tree absence has a dedicated typed condition; only it permits fallback, while tracked Git/path/digest failures reject before provider effect, including the explicit substitution regression |
| D-264-60 | review gate | during a second verification iteration, an older `round_results` entry for the same axis caused the lane barrier to ignore the newly accepted callback and replay continuation reconciliation against a finalizing child | `included` | the barrier now filters only an exact recorded axis plus verification iteration; unit coverage proves an older iteration remains collectable and an exact recorded iteration remains idempotently suppressed |
| D-264-61 | callback watchdog | accepted callback observation could return before healing the exact `sent` state plus `reserved` submit-receipt crash phase | `included` | accepted-receipt fast path first reconciles the exact mixed phase; runtime regression proves acceptance-before-restart heals once and the next generation remains usable without provider replay |
| D-264-62 | release evidence | the E6/E14 terminal tail was fixture-orchestrated through direct lifecycle helpers | `included` | a single effect-recorded fixture now enters through `ReviewGateController` and `RuntimeWorkerSummaryMixin.finish_task_summary`; direct-helper legacy coverage is excluded from the release receipt |
| D-264-63 | review gate tests | D-264-60 covered only the private iteration predicate | `included` | public current-review facade regression drives a two-lane Deep gate through initial findings, exact resolution, iteration-1 callbacks and terminal approval, with each retained continuation emitted once |
| D-264-64 | test harness | the pipeline-fix retry-intent fixture used a two-second eventual-read window that expired only under the complete `make test` load | `included` | the existing atomic-publication observer uses the established bounded ten-second test window; standalone task-summary and the complete exact-HEAD gate ladder are repeated |

## Deterministic candidate gates

Run on one committed candidate HEAD:

```bash
python3 tests/harness/test_callback_submit_recovery.py
python3 tests/harness/test_callback_submit_recovery_runtime.py
python3 tests/harness/test_liveness.py
python3 tests/harness/test_continuation_delivery.py
python3 tests/harness/test_release_blocker_runtime.py
python3 tests/harness/test_contract_state_edge_matrix.py
python3 tests/harness/test_harness_control_plane.py
python3 tests/harness/test_plan_review_facade.py
python3 tests/harness/test_review_inspect.py
python3 tests/harness/test_runtime_sessions.py
python3 tests/harness/test_runtime_task_summary.py
python3 tests/harness/test_task_escalation_records.py
python3 tests/test_task_lifecycle.py
bash tests/test_dcg_assets.sh
bash scripts/dcg-test-suite.sh
python3 tests/test_vault_link_repair.py
python3 tests/test_vault_schema.py
bash tests/test_stop_hook.sh
make test-harness
make test-harness-coverage
make test
make acceptance-check
python3 scripts/runtime-harness-lint.py
python3 scripts/validate-vault.py --summary
python3 scripts/codex-adapter.py --check
scripts/mcp-gateway/mcp-gateway.sh codex-sync --check
python3 references/upstream-skills/verify_snapshots.py
git diff --check v2.6.3..HEAD
```

The integrated pre-final-review run recorded 49 registered harness suites, 4,370
transition cases, 76.04% statement-line coverage across 128 modules
(13,934/18,324 lines), every critical floor green, and zero code-quality
release blockers/owned hotspots. The commands above are repeated after metadata
freezes and recorded in the ignored, content-addressed
`.vault-meta/release-evidence/<exact-head>/implementation-gates.json`, so the
terminal review can inspect exact-HEAD receipts without creating a
self-referential tracked commit.

## Review and external-effect boundary

The final route is single-model Codex/Sol because the user explicitly disabled
Anthropic/Fable/Opus review for this run. The review must cover intent and
engineering on exact HEAD, use the normal harness-owned callback path, and leave
no unowned reviewer resources. Full review is not selected automatically.

Push, tag, GitHub release, deployment and publication are outside this task and
require a separate user-authorized effect. The RC may be handed off only with
the exact HEAD, green command evidence, terminal review result, and any honest
remaining attention stated explicitly.
