# LLM Obsidian 2.6.5 intent evidence map

This map records the implementation evidence originally assembled for E1–E14.
It must be read together with
[`v2.6.5-accepted-review-waiver.md`](v2.6.5-accepted-review-waiver.md): the
technical gate is established, while independent review found several
production claims partial or contradicted. Those findings are accepted debt,
not silently green outcomes.

| Evidence | Integrated proof |
|---|---|
| E1 | `v2.6.5-causal-ledger.md` plus the D-264-73 and stale-`exiting` fixtures preserve the exact interleavings and root classes. |
| E2 | The new `ReviewAttempt` is immutable and exact-HEAD and generated v4 tasks enter the finalization ledger. Accepted debt: terminal exact attempts still publish a legacy `awaiting_resolution` authority. |
| E3 | The new exact-attempt path rejects cross-HEAD continuation. Accepted debt: V3/pre-activation compatibility and `review_drive_rearm` retain executable cross-HEAD paths. |
| E4 | The closed `ProviderEvent` vocabulary is the active interactive-runtime authority: provider start, durable input reservation/acceptance, schema-valid result, process exit and exact resource-close receipt advance one persisted cursor; public runtime and identity/cursor/gap matrices cover the path. |
| E4b | Claude print and Codex exec ephemeral adapters share one tested conformance contract. Accepted debt: production review does not yet route through that registry and still launches the interactive workspace path. |
| E5 | Provider delivery uses irreversible typed receipts; crash/restart tests prove no blind replay. |
| E6 | Typed delivery policy and bounded callback submit are tested. Accepted debt: legacy worker recovery can still use stable screen/elapsed time for submit and can restart a dead reviewer. |
| E7 | Finalization cycles reserve atomically, retain all prior attempts and refuse a sixth provider effect. |
| E8 | Finalization policy supports typed freshness-bounded availability. Accepted debt: production reservation currently supplies no availability evidence, so cycles 4–5 cannot reach the independent route. |
| E9 | PipelineSpec additions are optional and backward-compatible; finalization/Split audits and instruction/budget gates are green. Accepted debt: the semantic `tdd` change lacks its own improve-skills baseline/final verdict record. |
| E10 | Historical Subplan D artifacts prove preview-only zero effects and natural-count/one-child selection; `v2.6.5-split-activation-skill-audit.md` records the post-Gate explicit activation branch. |
| E11 | Split validation rejects digest, evidence, ownership, DAG, join, non-goal, pipeline and budget violations with all effect counters at zero. |
| E12 | `tests/harness/test_split_activation.py` proves bounded manifest-order waves, workspace-local execution/review/verification and dependency-only advancement through the existing dispatch adapter. |
| E13 | The Split store seals launch/terminal receipts and deterministic Join evidence. Accepted debt: ownership diff is still recomputed from a mutable base ref rather than a sealed base commit. |
| E14 | The immutable release attempt proves the technical gate on candidate `d159906d44fefb791c3e70c8914145228038ea71`: full suite, 76.10% honest coverage (`19,348/25,425`), 4,370 cases, acceptance, adapter/skill/quality gates, resource-free status, doctor `ok`, diff and clean status. It does not prove the deferred E2/E3/E4b/E6/E8/E9/E13 production claims; terminal review is waived explicitly. |

Subplans A-D started from exact base
`3e391fc9e6aa48e1344520dbffdebba704312540` and were adopted in deterministic
order at exact heads `571aa8d99dafaf12005be9a474862d8efb09e78d`,
`84ecb3bad198053468f1ceb15f69bbea06510e3a`,
`6575067e82340957bf85b8c1ee393f8cb59a661b`, and
`314c4a35b88701ce1d00e86a7cb817dbfcc443a4`. Every component commit and its
review/evidence disposition remains intact. A and D stayed in contained
lifecycle attention after their product evidence became durable, so their
providers were not replayed.

Stability Gate S passed on clean exact pre-activation HEAD
`023f69320b0833f8aac33a69f03d387ea2e485c1`: full tests, 76.65% honest harness
coverage with all critical floors and 4,370 generated cases, acceptance, vault
validation, quality, skill, instruction, adapter/MCP, diff and clean-status
checks are bound by immutable [attempt
`0043d16e-35b6-4bd9-bc90-d365a32d4841`](evidence/v2.6.5/gate-s-023f693/receipt.json).
This is an exact-tree reconstruction after activation, not a claim about
historical live resources; activation ancestry and the live Split record
establish the ordering, while the final gate owns current resource inventory.
The bounded live activation and its mechanism-recovery boundary are recorded
in `v2.6.5-live-split-dogfood.md`. Final release attempt
`26500000-0000-4000-8000-0000d1599060` passed the complete immutable profile on
clean exact integrated candidate `d159906d44fefb791c3e70c8914145228038ea71`
and owns the captured resource inventory. The 2026-08-06 release waiver accepts
the absent terminal implementation/release approval and binds every independent
review finding to the 2.6.6 remediation plan.
