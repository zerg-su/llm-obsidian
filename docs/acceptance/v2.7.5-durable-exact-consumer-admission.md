# 2.7.5 durable exact consumer admission — exact candidate evidence

Scope: the bounded 2.7.5 closure of exactly the frozen
`F274.POST_CHECK_LAUNCH_RACE` class from the preserved 2.7.4
negative-evidence candidate base `c06de4505cd1768aca17a9dcac0e24cf95e9bc23`.
The two accepted 2.7.4 fixes — the bracketed current-candidate observation
and the identity-exact wait-only review-resolution marker — are preserved and
not replayed; the cancelled 2.7.4 root is not reopened. This note claims no
review acceptance, reap, merge, plugin installation, dogfood, tag, or
publication.

## Closed finding (frozen 2.7.4 terminal evidence, immutable)

- `F274.POST_CHECK_LAUNCH_RACE`: the current-candidate predicate was checked
  before, but not bound to, the durable controller-link and review-launch
  effects, so a clean commit landing strictly after the predicate's closing
  HEAD read and before those effects could still link stale authority or
  launch review at an unverified HEAD.

## Repair (candidate commits on the task branch)

- `3052a4a8` — RED (`E275.POST_CHECK_RED`): deterministic hermetic
  injections land a clean commit strictly after the closing candidate read —
  the injection binds to the module-level current-candidate predicate itself,
  so it fires exactly once at the last observation the production code
  makes — and before (a) controller-link publication
  (`tests/harness/test_verification_authority.py`) and (b) the review
  launch (`tests/harness/test_runtime_task_summary.py`). At the base both
  consumers fail: the stale link is published and the provider launcher is
  released. Unchanged exact clean candidates keep the ordinary link and
  launch paths as in-file controls.
- `3d58c693` — GREEN (`E275.EXACT_CONSUMER_ADMISSION`), 149 measured
  production lines across three existing owners, no new module, lock,
  schema, CLI, callback rewrite, or authority owner:
  - `scripts/harness/runtime_worker_verification.py` (+15):
    `link_verification_receipt` is now the single admission boundary for
    controller authority. A completed receipt is linked only while its exact
    receipt HEAD is the clean current candidate, re-observed through the
    existing bracketed predicate immediately before the atomic write and
    once more after it; a post-write mismatch retracts the link, so no
    schedule that moves the HEAD after any earlier observation leaves a
    durable stale link. A failed receipt is failure evidence for the
    existing attention/resubmit machinery, not authority, and stays linkable
    regardless of currency. Both callers (link recovery and in-effect
    persistence) route through this one boundary; the receipt itself remains
    immutable evidence for its own exact HEAD and is never rewritten.
  - `scripts/harness/runtime_worker_review_bridge.py` (+65):
    `drive_review` no longer carries its earlier boolean observations into
    the launch. `_publish_review_launch_admission` re-reads the durable
    verification receipt for the bound HEAD, re-observes that exact HEAD as
    the clean current candidate, and publishes the admitted pair —
    verification operation/lane/run identity, the canonical receipt SHA-256,
    and the exact HEAD — as `review-launch-admission.json` in the shared
    review-runtime root, immediately before releasing the launch. Any
    receipt, HEAD, or clean-state mismatch refuses the launch with zero
    provider effect and leaves prior evidence untouched. Drives without a
    bound verification contract keep their existing gates and any previously
    admitted pair.
  - `scripts/task_review_flow.py` (+69): `_admitted_review_launch` verifies
    the published admission against the actual review context inside the
    existing exact-HEAD flow, before `reserve_exact_head_attempt` and before
    any provider effect. The admission must be schema-exact for this task,
    name exactly the context's HEAD, and that HEAD must still be the clean
    current candidate; any HEAD, receipt-identity, clean-state, or
    launch-input mismatch fails closed, and a malformed or symlinked
    admission is never treated as absent. Absence keeps launches without a
    pipeline verification owner (and pre-admission records) on their
    existing gates. `scripts/task_review_context.py` is unchanged: the
    context manifest already binds the actual observed HEAD, which is
    exactly what the gate compares against.

- The configured Sol XHigh implementation review of `b65b736f` reported two
  material findings, both applied in one bounded findings patch (the final
  candidate commit). `F275.RECEIPT_ADMISSION_NOT_CONSUMED`: the flow gate no
  longer trusts the admission's self-asserted digest — it re-reads the
  durable receipt named by the admission's published absolute
  `receipt_pointer` (shape-confined to
  `<state-root>/pipeline-verification/<admitted-id>/receipt.json`, resolved,
  non-symlinked) through the same `VerificationAuthority.load` ingress the
  verification owner uses, bound to the one dispatch record discovered by
  exact operation identity across the store's owner directories
  (unreadable or ambiguous candidates fail closed), the dispatch contract's
  compiled definition, and the frozen task verification profile; the
  canonical receipt digest and the receipt's operation/lane/run/parent/
  status/HEAD identities must equal the admission exactly. A dispatch whose
  compiled contract owns a verify step must present the admission — absence
  is fail-closed, never compatibility — so deleting the admission or the
  receipt refuses the launch, while contracts without a verification owner
  (and records the store does not know) keep their existing gates.
  `F275.POST_CHECK_PROVIDER_RACE`: the same admission closure is threaded
  through `begin_attempt` into the gate's launch transaction
  (`scripts/harness/workflows/review_gate_attempt.py`,
  `scripts/harness/workflows/review_gate.py`,
  `scripts/harness/workflows/review.py`) and re-runs as the last observation
  immediately before each fresh `runtime.start`, after reservation; drift
  refused there aborts with zero provider starts, zero operation rows, and
  an attention-marked attempt that the existing zero-effect recovery owns.
  The patch costs 199 measured lines across the same consumers and the
  three gate owners; no production module, schema, CLI, lock protocol, or
  authority owner is added (the admission gains only the code-owned
  `receipt_pointer` field, mirroring the existing verification attention
  packet).

## Fail-closed matrix (hermetic, at the candidate)

- A clean commit after the closing candidate read and before controller-link
  publication publishes zero stale link, returns the receipt as immutable
  evidence with byte-identical durable bytes, and the unchanged exact clean
  candidate keeps ordinary link publication
  (`tests/harness/test_verification_authority.py`).
- A clean commit after the closing candidate read and before the review
  launch releases zero provider effect, writes no started marker, and admits
  no stale pair; the unchanged exact clean candidate launches exactly once
  and publishes the exact receipt/HEAD admission
  (`tests/harness/test_runtime_task_summary.py`).
- The flow-side admission gate: a verify-owning contract with a missing
  admission fails closed, while contracts without a verification owner and
  recordless tasks keep their existing gates; the exact durable receipt/HEAD
  pair admits a clean launch; a digest that disagrees with the durable
  receipt, a foreign lane identity, a receiptless verification identity, a
  deleted or mutated durable receipt, a relative or identity-mismatched
  receipt pointer, a context bound to another HEAD, a post-admission clean
  commit, same-HEAD dirt, and a present admission without a resolvable
  dispatch contract are all refused before reservation; foreign-task,
  schema-drifted, unadmitted, short-digest, empty-identity, extra-field,
  malformed, and symlinked admissions all fail closed rather than being
  treated as absent (`tests/harness/test_task_review_flow_units.py`).
- The provider-start boundary: a refused launch admission inside the gate's
  launch transaction starts zero providers, creates zero operation rows, and
  leaves an attention-marked effect-free reservation; the unchanged exact
  clean candidate admits exactly once immediately before each fresh provider
  start (`tests/harness/test_exact_head_review_attempt.py`).
- Inherited 2.7.1–2.7.4 authority, orphan-census, clean-commit race,
  resolution-marker, retry, cleanup, routing, contract, callback, and crash
  matrices stay green, including the full corridor simulator, whose real
  bridge-published admissions are consumed by the real runner gate.

## Exact commands (rerun from the candidate tree root)

- `python3 tests/harness/test_verification_authority.py`
- `python3 tests/harness/test_runtime_task_summary.py`
- `python3 tests/harness/test_task_review_flow_units.py`
- `python3 tests/harness/test_exact_head_review_attempt.py`
- `python3 tests/harness/test_review_transport.py`
- `python3 tests/harness/test_callback_submit_recovery.py`
- `python3 tests/harness/test_callback_submit_recovery_runtime.py`
- `python3 tests/harness/test_callbacks.py`
- `python3 tests/harness/test_lifecycle_crash_matrix.py`
- `make test-harness`
- `make test-model-routing`
- `python3 scripts/code-quality-audit.py --rc1-authority-json`
- `python3 tests/test_code_quality_audit.py`
- `python3 tests/harness/test_v266_rc1_integration.py`
- `make test-docs`
- `make test-code-quality`
- `python3 tests/rc4_scope_ratchet.py`
- `python3 scripts/task_contract.py validate`
- `git diff --check`

## Final active-authority contour

Measured once after production code was final, over the same separately
owned 27-file manifest (`config/active-review-authority.json`; membership
unchanged):

```json
{
  "production_loc": 15599,
  "production_file_count": 27,
  "writable_authority_count": 0,
  "incident_literal_count": 0
}
```

`config/code-quality-baseline.json`
`rc1_active_authority.final_max_production_loc` is bound to exactly `15599`
— the measurement of this candidate after the applied findings patch, not a
growth allowance; `tests/harness/test_v266_rc1_integration.py` asserts exact
equality, so both growth and stale headroom fail. The owned file-lines
hotspots move to exactly the measured 1276 (verification owner), 1146
(review bridge), 1438 (review flow), and 1051 (gate attempt owner) lines,
and `_run_exact_head_review` moves to exactly its measured 455 lines. The
live scripts scope ratchet moves by the measured 149-line closure cost (15
owner + 65 review bridge + 69 review flow) plus the measured 199-line
findings-patch cost to the exact 290-file / 109,263-line candidate with no
speculative headroom.

## Complete-gate evidence bound to the exact candidate HEAD

As established by the 2.7.3 review (`F273.EXACT_HEAD_GATE`), a committed
gate receipt can only ever describe a predecessor tree. Exact-HEAD
completion evidence for this candidate is therefore recorded
owner-externally, where recording it does not move the product HEAD:

- `.task-pipeline/outputs/pass-0/candidate-gate-receipt.json` (task-pipeline
  runtime state, repository-ignored, reviewer-observable in the task
  worktree) binds one complete `make test-harness` run — exit code, output
  hash and line count, and registered suite count — to the exact resolved
  final HEAD observed clean immediately before and after the run.
- The Harness's own scoped verification receipt for this dispatch
  (`pipeline-verification/<child>/receipt.json` in the owner store) binds
  the configured verification profile to the same exact resolved HEAD
  through the schema-v2 verification authority.
