# LLM Obsidian v2.6.6 RC3

RC3 is the final evidence-polish candidate over exact RC2 commit
`b86a33d779bd8852915a4b875f12ef9a9b7366b3`. It closes five carried review
findings and aligns public documentation with the runtime that remained after
the RC1/RC2 deletion work. It adds no provider route, scheduler, state machine,
lifecycle owner, background model call, or synchronization capability.

## Reproducible release evidence

- `rc3_inventory.py` publishes a post-commit external sidecar and rejects
  changed OIDs, counters, topology, authority counts, dispositions, or an
  embedded candidate SHA that differs from the independently supplied HEAD.
- `rc3_slice_receipt.py` publishes immutable prospective A–F receipts. It
  explicitly rejects fabricated RC2 backfill.
- Two live exact-tree coverage observations produced the same
  `16855/22751` counters, `74.08%` weighted coverage, ten passing critical
  floors, and 4,370 transition-matrix cases.
- Shell suites allocate scratch space below `TMPDIR` through one scoped helper.
- `verification_receipt.py` reserves each full-profile execution in an
  append-only ledger before the first command. `rc3_release_disposition.py`
  validates actual gate outputs, review callback/meta pairs, and finding-proof
  bytes before compiling the schema-checked terminal result.

The executable mapping from `RC3-E1` through `RC3-E9` is in
[`v2.6.6-rc3-evidence-contract.md`](../acceptance/v2.6.6-rc3-evidence-contract.md).
The release handoff and local-only commands are in
[`v2.6.6-rc3-release-readiness.md`](../acceptance/v2.6.6-rc3-release-readiness.md).

## Plan and documentation contract

Normative implementation plans now default to English unless the user
explicitly requests another language. The Outcome Contract, evidence IDs,
slices, stop rules, and verification instructions remain mandatory. Public
runtime documentation now names the Harness, callback broker, runtime worker,
and lifecycle wrapper that own their actual boundaries, without reviving the
removed classic supervisor/watchdog contour.

## Release boundary

This checkout prepares a local verified candidate only. It does not push, tag,
publish, install, or merge. Publication remains a user action after the exact
release-final receipt and both configured review verdicts compile to an
approved disposition.
