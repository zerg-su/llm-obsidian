---
type: repo
title: "2.5 real dogfood RT01 stale operation reconciliation"
address: c-000045
created: 2026-08-01
updated: 2026-08-01
tags:
  - reap
  - repo
status: active
sessions:
  - "019fab00-3160-7380-8920-4b20183afb76"
executor_runtime: codex
executor_model: "gpt-5.6-sol"
related:
  - "[[Cross-model review — 8d9f0b29-0f04-4f32-8484-f203ab212b81 — d877febeeedb]]"
---

# 2.5 real dogfood RT01 stale operation reconciliation

## Outcome

Committed `530f020c427ea330ae6bc025fd008b629bf638cb` (`fix: classify terminal legacy operations as stale`). Upgrade preflight now uses exact authoritative harness identity to exclude only same-ID legacy worktree and broker mirrors proven terminal, effect-settled, resource-free, and lane-free. It performs no lifecycle mutation.

## Diagnosis

Cancellation correctly terminalized the two old dispatches and cleared their owned provider, supervisor, and cmux resources, but successful reap exclusively owns legacy broker archival and `.task-reap-complete.json`. Preflight classified those unreaped mirrors independently and discarded the terminal harness fact, so proven-dead residue was mislabeled active.

## Safety and regression

The regression covers same-ID v3 worktree metadata, an active legacy broker mirror, and a terminal harness dispatch. Resource-free settled terminal state proves the mirrors stale; residual effects/resources remain blockers. Unmatched, malformed, nonterminal, lane-bearing, degraded/archiving, review, and research state remains fail-closed.

## Verification

- `make test-upgrade-preflight` passed, including the live/uncertain control and proven-stale case.
- The original source-root `upgrade-preflight` loop still exits `4` for current live or uncertain operations and no longer reports stale identities `8714…` or `930c…`.
- Coordinator-accepted host verification: `make test-harness` exited `0` at exact HEAD `530f020c427ea330ae6bc025fd008b629bf638cb`, resolving the executor sandbox's `AF_UNIX` permission boundary as the required scoped evidence.
- `git diff --check` passed before commit.

Only `scripts/upgrade-preflight.py` and `tests/test_upgrade_preflight.py` are committed; no vault, source checkout, lifecycle record, worktree, branch, or external state was modified.

Review archive: [[Cross-model review — 8d9f0b29-0f04-4f32-8484-f203ab212b81 — d877febeeedb]]
