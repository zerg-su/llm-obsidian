---
type: review
status: active
created: 2026-07-31
updated: 2026-07-31
tags: [review, harness]
sessions: []
review_id: "232fe53b-d609-4467-9a9d-13e45326368b"
address: "c-000040"
---

# Cross-model review — 232fe53b-d609-4467-9a9d-13e45326368b — ffe07464dfd5

Final verdict: `approve`.

## Bound evidence

- Operation: `232fe53b-d609-4467-9a9d-13e45326368b`
- Run: `b769c311d39acc06d85d8f39a8b239c7`
- Mode: `simple`
- HEAD: `99d56159ab66ed8b07e141ac8b42e26e0a8f7270`
- Verification profile: `scoped` (`7724ff1e1e5dd664008315264d34a55bd53d35d9d10b66d797c540841108df82`)

## Axis: holistic

- Verdict: `approve`
- Verification iteration: 0

### Findings

- **holistic-1 · minor · The ADR defers every improvement to live paired evidence, including the diff-completeness gap its own prototype proves deterministically.**
  - File: `docs/decisions/rt03-same-session-review-delta-packet.md:111`
  - Evidence: scripts/task-review-runner.py:337-350 packages only `git show ... HEAD` for the verification packet, and scripts/task-review-runner.py:918-932 already validates `awaiting_resolution[axis]["reviewed_head_sha"]` before continuing the same session, so the reviewed boundary is available at packet-build time. The prototype's `head_only_packet_misses_earlier_fix` and `multi_commit_resolution_covered` checks (prototypes/rt03-review-delta-packet.py:271-279) demonstrate the omission without any model-quality assumption. Diff coverage is a deterministic property, so the ADR's own success criterion for that sub-change is met, yet the Decision section rejects all production change.
  - Recommendation: Separate the two claims in the Decision section: keep the full anchored delta packet deferred pending live paired review, but state explicitly that packaging the reviewed-to-resolved range when the gate supplies `reviewed_head_sha` is already proven and is a candidate narrow fix, so the deferral does not silently park a known evidence gap.
- **holistic-2 · minor · Four of the six prototype checks cannot fail by construction, so the evidence section reads stronger than the experiment is.**
  - File: `docs/decisions/rt03-same-session-review-delta-packet.md:82`
  - Evidence: `deterministic_packet` (prototypes/rt03-review-delta-packet.py:259) compares two builds in one process, but scripts/harness/context.py:129-135 already sorts inputs and metadata, so the two manifests are byte-identical by definition and both builds even write into the same packet directory. `bounded_oracle_verdict_matches_exact_tree` (prototypes/rt03-review-delta-packet.py:280-283) compares two hand-written predicates over the same hardcoded fixture. `original_context_exactly_bound` and `review_boundary_exactly_bound` restate metadata the script just supplied. Only the two coverage checks add information. The ADR's limitation paragraph is honest about live review quality but does not distinguish falsifiable checks from tautologies.
  - Recommendation: Mark which checks are falsifiable, or add one genuinely negative case (unrelated-ancestry rejection, or a cross-process replay comparing packet_id) so the determinism claim is actually tested rather than restated.
- **holistic-3 · minor · Required field 4 mandates fail-closed on budget overflow, which contradicts the existing runner's degrade-with-marker behavior without weighing the tradeoff.**
  - File: `docs/decisions/rt03-same-session-review-delta-packet.md:112`
  - Evidence: scripts/task-review-runner.py:346-350 truncates an oversized diff to 65,000 bytes and appends `[diff truncated; inspect product HEAD]`, keeping the gate moving because the reviewer can still inspect the exact read-only HEAD. The ADR asserts the candidate must "fail-closed on invalid ancestry or budget overflow rather than silently truncating". Under that rule a large but legitimate resolution range would leave the gate at `awaiting-resolution` (see the Control flow section, line 130) instead of degrading, which is a behavior regression relative to today.
  - Recommendation: State the tradeoff: fail-closed is right for invalid ancestry (a correctness error), but for byte overflow prefer an explicit bounded marker, or justify why stranding the gate is preferable to today's marker-plus-HEAD-inspection path.
- **holistic-4 · minor · The documented reproduction command can fail opaquely on a normal developer machine because git stderr is discarded and the fixture is not isolated from global git config.**
  - File: `prototypes/rt03-review-delta-packet.py:48`
  - Evidence: `run` raises `RuntimeError("bounded Git fixture failed")` and drops `result.stderr` (prototypes/rt03-review-delta-packet.py:39-49). The fixture sets only `user.email`/`user.name`, so a global `commit.gpgsign=true`, `core.hooksPath`, or `commit.template` makes `commit` fail with no diagnosable output, while docs/decisions/rt03-same-session-review-delta-packet.md:73-75 presents this script as the ADR's reproducible evidence.
  - Recommendation: Include `result.stderr` in the raised message and run the fixture with `-c commit.gpgsign=false -c core.hooksPath=` (or a scoped GIT_CONFIG_GLOBAL) so the ADR's evidence stays reproducible outside the author's environment.
- **holistic-5 · minor · A new top-level `prototypes/` directory lands in the tree with no stated lifecycle, ownership, or test coverage boundary.**
  - File: `prototypes/rt03-review-delta-packet.py:1`
  - Evidence: This is the only file under `prototypes/`, and the directory appears in neither the CLAUDE.md nor the AGENTS.md repository layout. No Makefile target runs it and scripts/model-literal-lint.py:19 scans only `scripts`, `skills`, `agents`, `hooks`, `.claude`, so it is outside every gate. skills/prototype/SKILL.md:15-18 says the artifact remains off main and that promotion requires separate authorization, while docs/decisions/rt03-same-session-review-delta-packet.md:75 treats the script as durable evidence. Keeping it is defensible; leaving its status unstated is the gap.
  - Recommendation: Add one line to the ADR (and ideally the repo layout section) declaring that `prototypes/` holds disposable, unmaintained decision evidence excluded from `make test`, and name the condition under which this file is deleted.

## Verification gaps

- None

## Residual risks

- None

## Notes for executor

- None

## Archive boundary

Raw prompts, transcripts, commands, sockets, and cmux/process identifiers are excluded.
