interface:
  display_name: "Split"
  short_description: "Preview or explicitly activate governed Split manifests"
  default_prompt: "Use $split for a zero-effect preview, or $split --dispatch for approved harness activation."
policy:
  allow_implicit_invocation: false
